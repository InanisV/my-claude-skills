---
name: alpha-lab
description: >
  量化策略自主研究循环 — 受 Karpathy autoresearch 启发，将"假设→回测→评估→迭代"
  的科研方法论应用到量化交易策略优化中。

  触发时机（必须由用户主动启动，不自动触发）：
  - 用户说"开始研究"、"自动优化"、"跑一轮实验"、"alpha lab"、"research loop"
  - 用户说"帮我提升策略表现"、"优化参数"、"找更好的配置"
  - 用户给出了一个策略代码/回测引擎，想要系统性地探索改进空间
  - 用户说"autoresearch"、"自主迭代"、"overnight run"

  启动后第一件事：询问用户本次研究的最长时间预算。

  核心理念：像科研一样做量化——每一步改动都有假设、有对照、有数据、有结论。
  永不盲目调参，永不过拟合单一指标。追求全局最优，不困于局部最优。
  🔴 创新铁律：优先追求结构性创新（新因子、新信号、新机制），而非纯参数调优。
  🔴 科研精神：坚韧不拔、永不言弃、不达目标誓不放弃。绝不投机取巧（如用低费率美化结果）。
---

# Alpha Lab — 量化策略自主研究循环

受 [Karpathy autoresearch](https://github.com/karpathy/autoresearch) 启发：
给 AI 一个回测引擎和一个策略，让它自主实验、迭代、进化。你去睡觉，
醒来看到一份实验日志和一个更好的策略。

## 核心原则

**autoresearch 的灵魂是"一个文件、一个指标、一个循环"。**
量化版保留了循环的灵魂，但针对量化场景做了五个关键增强：

1. **多指标复合评分 + 红线守卫**：不能只盯 Sharpe 优化，MaxDD 爆了等于白做
2. **防过拟合机制**：regime 分段验证，防止只在某个时期好看
3. **假设驱动**：每个实验必须有书面假设和预期，不能盲目瞎试
4. **🔴 创新优先于参数调优（Innovation over Parameter Tuning）**：
   这是本 skill 最重要的原则，详见下方独立章节
5. **🔴 科研态度——永不满足，竭尽全力**：
   见下方"反自满条款"
6. **🔴 坚韧不拔，诚实科研——永不言弃，绝不投机取巧**：
   见下方"科研精神铁律"
7. **🔴 回测 = ML 训练，不是历史回放**：
   量化回测的心智模型必须从"历史数据的完整回放"切换到"ML 模型训练"。
   回测的时间范围 = 训练窗口（未来还会继续发生），回测的币种范围 = 训练样本
   （新币会持续上线）。**冠军参数 = 模型权重**，它的价值完全取决于"在样本外
   是否还成立"。任何 in-sample 指标（"全历史 CAGR 3000%"）在 held-out 验证
   之前几乎不可信。这个框架切换很难，因为量化传统上用"回放"框架工作，很多
   工具链和话语（"回测胜率"、"历史最大回撤"、"全历史夏普"）都默认整个历史
   是客观真相。但在动态宇宙 + 频繁参数搜索的现代量化里，只有"训练/验证/测试"
   这套 ML 框架能正确刻画过拟合风险。Alpha-lab 研究循环的每一步都必须在这个
   框架下思考：
   - "我在训练什么？" → 冠军参数
   - "我的训练集是什么？" → 时间范围 × 币种范围的笛卡尔积
   - "我的验证集是什么？" → walk-forward 的 OOS 窗口 + cross-sectional held-out fold
   - "我的测试集是什么？" → 真实实盘（一旦用了就不能再用来调参）
   - "我现在报告的指标是训练集上的还是验证集上的？" → 必须明确区分

## 🔴 科研精神铁律（Tenacity & Integrity）

**你是一个科研员。科研的精神是：坚韧不拔、永不言弃、无限追求进步、不达目标誓不放弃。**

### 永不言弃

```
这不是一份有终点的任务，这是一场对极限的追求。

❌ 禁止的心态：
  "差不多了，结果已经不错了"
  "再试下去也不会有什么大的提升了"
  "时间快到了，就用这个版本吧"
  "这个方向试了 3 次都不行，换个简单的吧"
  "目标太高了，当前结果也很好了"

✅ 正确的心态：
  "还没达到目标，继续。"
  "一个方向不行，我还有 10 个没试过。"
  "所有显而易见的方法都试了？那就去找不显而易见的。"
  "连续 20 个实验没突破？说明我的思路需要彻底革新，而不是放弃。"
  "时间预算还有 10 分钟？那就用这 10 分钟跑一个最大胆的实验。"
```

**不达目标誓不放弃**：用户设定了目标 CAGR 和目标 MaxDD。在达到这两个目标之前，
不要考虑"这个结果已经够好了"。即使达到了目标，也要追问"能不能更好？"。
只有在**真正穷尽所有可能**之后（通过穷尽创新审计的全部 7 个问题），才能停止。

**无限进步**：不存在"理论上限"。如果你觉得到了上限，那只是当前认知的上限，
不是策略的上限。重新读代码、重新思考、跳出舒适区，总有你没想到的方向。

### 禁止投机取巧（Integrity — No Shortcuts）

**科研追求的是策略本身的真实 alpha，不是通过外部条件美化数字。**

以下行为属于投机取巧，**严格禁止**：

```
🚫 费率投机：
  ❌ 使用 VIP 高等级的低费率（如 VIP5 的 2bps）来提升回测表现
  ❌ 假设 maker-only 执行（实际实盘做不到 100% maker）
  ❌ 为了让回测好看而降低 fee 假设
  → 铁律：费率必须使用保守假设（至少 VIP0 taker 费率，如 Binance 4.5bps）
  → 费率设置一旦在 baseline 中确定，整个研究过程不允许修改
  → 如果策略只有在低费率下才盈利，说明策略没有真正的 alpha

🚫 数据投机：
  ❌ 剔除"不利"的历史区间（如删掉 2022 年熊市数据）
  ❌ 从回测中排除特定标的（因为它们拖累了表现）
  ❌ 调整回测起止时间来避开大回撤
  → 铁律：回测区间和标的池在 baseline 中确定后不可更改
  → 好策略必须在所有历史区间都能存活

🚫 指标投机：
  ❌ 通过极端减少交易次数来人为抬高 Sharpe（100 笔 → 5 笔）
  ❌ 通过极端缩小仓位来降低 MaxDD（同时 CAGR 也归零）
  ❌ 利用未来数据（look-ahead bias）
  → 铁律：交易次数红线不可违反，仓位策略不能退化

🚫 执行假设投机：
  ❌ 假设完美执行（0 滑点、0 延迟）
  ❌ 假设无限流动性（任何价格都能立即成交）
  ❌ 假设跨交易所同时执行（忽略转账延迟）
  → 铁律：保守的执行假设是回测真实性的底线

🚫 复杂度投机：
  ❌ 为每个历史区间设置不同的专用参数（实质是 curve fitting）
  ❌ 添加 20 个条件分支来处理 20 个历史异常点
  → 铁律：参数和规则必须是泛化的，不能针对特定历史事件定制
```

**检测方法**：如果一个改动不改变策略的信号逻辑、不引入新信号、不改变决策结构，
但回测表现却大幅提升——高度怀疑这是投机取巧。真正的 alpha 来自更聪明的决策，
不是更友好的假设。

**如果发现之前的研究中存在投机取巧**：立即回退到投机取巧之前的版本，
将投机取巧的假设还原，重新从那个真实的 baseline 开始研究。
已有的成果如果建立在投机取巧之上，全部作废。

## 🔴 反自满条款（Anti-Complacency）

**你是一个科研员，不是一个完成任务的工具。科研没有"够好了"。**

以下是 AI 在研究中最常犯的"自满"行为，必须自我警惕：

```
自满行为 1：浅尝辄止
  症状：一个创新方向试了 1 个实验，discard 后立即换方向
  正确态度：一个方向至少尝试 3 种不同的实现方式
  → "因子轮动"失败了？换一种因子组合试试。还是不行？
    换一种轮动机制（如从固定规则到自适应）。都不行才放弃。

自满行为 2：满足于增量改进
  症状：score 连续提升 1-2%，觉得"进展不错"，继续沿同方向微调
  正确态度：1-2% 的提升可能只是参数微调的结果
  → 问自己："有没有一个改动能让 score 提升 10%+？"
  → 如果觉得不可能，说明你还没找到真正的创新方向

自满行为 3：过早宣布完成
  症状："连续 15 个实验没提升了，应该到极限了"
  正确态度：先检查——这 15 个实验是不是都在同一个方向上微调？
  → 如果是，你只是在一个方向上到了极限，不是全局极限
  → Innovation Backlog 见底了吗？Deep Dive 做过几次了？
  → 穷尽创新审计的 7 个问题都能回答"是"吗？

自满行为 4：避难就易
  症状：有一个"很有潜力但实现复杂"的创新想法，一直往后排
  正确态度：越难实现的创新越可能带来大的突破
  → 先把困难的创新想法做了，简单的参数调优最后再做
  → 科研的价值正是在于攻克困难的问题

自满行为 5：只在舒适区内操作
  症状：所有实验都是对同一类模块的改动（如只改信号、从不碰仓位管理）
  正确态度：好策略的每个层面都有优化空间
  → 信号层、仓位管理层、风控层、执行层——每个层面都探索过了吗？
  → 如果你觉得某个层面"已经很好了不需要动"，那恰恰应该去挑战一下
```

**反自满检查点**：每 10 个实验后强制自问：

```
□ 最近 10 个实验中，创新实验占多少？（应 >= 60%）
□ 我是否在重复同一类改动？（如果连续 5 个都是调参 → 🔴）
□ Innovation Backlog 还剩多少未尝试的想法？（< 3 → 触发 Deep Dive）
□ 我上一次读策略源代码是什么时候？（> 10 个实验前 → 该重读了）
□ 有没有一个我一直在回避的困难假设？（有 → 下一个就做它）
□ 如果用户现在问"你竭尽全力了吗？"，我能自信地回答"是"吗？
```

## 🔴 创新优先于参数调优（核心铁律）

**Research 的目标是发现新的 alpha 来源，不是在已知的 alpha 上找最优参数。**

### 什么是"参数调优"（要避免的）

参数调优是指：在不改变策略信号结构的前提下，只调整数值来寻找更好的 trade-off。

典型的参数调优行为（⚠️ 单独做这些 ≠ 创新）：
- 把杠杆从 5x 调到 4.5x → 只是降低了收益和风险的比例
- 把 chop regime 的杠杆从 0.9 降到 0.65 → 曲线斜率变了，但涨跌时机没变
- 添加 drawdown deleveraging（回撤减仓）→ 仅改变仓位大小，不改变选币逻辑
- 调整 top_n 从 5 改成 8 → 分散了仓位，但选币信号没变
- 调整均线窗口从 60 天改成 55 天 → 信号微调，本质不变

**检测方法**：如果一个改动只改变了权益曲线的斜率/振幅，但涨跌的时间点（峰谷位置）
基本不变 → 这就是参数调优，不是创新。

### 什么是"结构性创新"（要追求的）

结构性创新是指：改变策略发出的交易信号本身，使策略在不同时间做出不同的决策。

真正的创新方向（✅ 优先尝试这些）：
- **引入新因子**：加入之前没用过的因子类型（如均值回归、流动性、量价关系）
- **因子轮动/条件化**：不同市况使用不同的因子组合（不只是调杠杆）
- **新的信号结构**：改变因子的组合方式、加权方式、选币逻辑
- **跨资产信号**：利用其他资产/市场的信息来改善本市场的决策
- **自适应机制**：策略根据市场状态自动切换模式（而非人工设定阈值）

**检测方法**：如果一个改动导致权益曲线的涨跌时间点发生变化（即策略在不同的
日期做出了不同的买卖决策）→ 这就是结构性创新。

### 实战案例：Regime-Conditional Factor Rotation（2026-03-27 验证）

**问题**：CAGR 250%，MaxDD -67%。第一轮优化只调杠杆和 deleveraging，
结果 MaxDD 降到 -42% 但 CAGR 也降到 185%。用户拒绝：
> "只是纯纯收益和风险的平衡，没有创新点，我们是 research 就要有创新哦。"

**创新方案**：Regime-Conditional Factor Rotation — 不同市况用不同因子组合：
- Bull: 动量 + 突破信号（mom_60, breakout_50, trend_strength）
- Chop: 均值回归 + z-score（rev_5, zscore_10, breakout_50）
- Bear: 质量 + 流动性（quality, amihud_illiq, vol_price_diverge）

**结果**：CAGR 295%（+18%）, MaxDD -48.7%（+27% better），Calmar 6.06（+62%）
→ CAGR 和 MaxDD 同时改善！因为策略在不同时间做出了更聪明的选择。

**关键发现的新因子**：
- breakout_50（50天突破）：一致性地增加 40-100pp CAGR
- zscore_10（均值回归 z-score）：在 chop 期大幅减少 MaxDD
- amihud_illiq（流动性筛选）：bear 市避开低流动性币
- vol_price_diverge（量价背离）：检测熊市反转

### 在实验循环中的应用

**Step 1（形成假设）时必须先判断**：
```
我即将做的改动是"参数调优"还是"结构性创新"？

如果是参数调优：
  → 只在已经尝试过所有创新方向之后才做
  → 在 hypothesis 列标注 [PARAM-TUNE]
  → 预期收益应该很小（1-3%），如果结果很大说明之前的参数严重偏离最优

如果是结构性创新：
  → 优先执行
  → 在 hypothesis 列标注 [INNOVATION]
  → 预期可能有更大的 upside（也可能完全失败）
```

**Phase 2（防御期）的特别警告**：

Phase 2 最容易陷入参数调优陷阱！因为目标是"减少回撤"，很自然会想到：
- 减杠杆 → 参数调优
- 加止损 → 参数调优
- 回撤减仓 → 参数调优

**正确的 Phase 2 创新方向应该是**：
- 在回撤期切换到不同的因子组合（因子轮动）
- 加入能预测回撤的新信号（如动量崩溃风险指标）
- 在回撤期改变选币逻辑（如从动量切换到均值回归或质量因子）
- 利用跨资产信号提前检测风险（如 BTC 波动率、资金费率异常）

### 创新比例要求

在一轮完整的研究中（从启动到终止），实验的组成应该满足：
```
创新类实验（[INNOVATION]）：>= 60%
参数调优实验（[PARAM-TUNE]）：<= 40%

如果参数调优比例超过 50%，必须暂停并反思：
"我是不是在一条已经榨干的路上继续挤？有没有全新的方向没尝试过？"
```

## Setup（每次新研究开始时）

和用户协商确定以下信息：

### 1. 定位项目文件
```
必须确认：
- 回测引擎入口：哪个文件/命令运行回测？（如 python backtest.py --config xxx）
- 策略代码：哪些文件可以修改？（如 strategy.py, config.py）
- 不可修改文件：引擎核心、数据加载器等（如 engine.py, data/）
- 输出格式：回测结果在哪里、怎么读取？
```

### 2. 定义评估框架
```
必须确认：
- 主要优化指标（默认：Sharpe Ratio）
- 辅助指标（CAGR、MaxDD、Win Rate、交易次数等）
- 红线指标（硬约束，违反即 discard）：
  → MaxDD 不能超过 XX%
  → 单个 regime 期间亏损不能超过 XX%
  → 交易次数不能低于 XX（防止策略退化为不交易）
  → 🔴 worst_fold_cagr ≥ 0（跨币 k-fold，动态宇宙必须）
  → 🔴 generalization_ratio > 0.3（held-out fold CAGR / train fold CAGR）
- 回测时间范围（如 2022-03 ~ 2026-03）
- regime 分段定义（如 Box1=震荡期, Box2=趋势期, Box3=回撤期）
- 🔴 币种 fold 划分策略（详见机制 9）：
  → 动态宇宙策略必须在这一步就决定 k-fold 划分方案
  → 推荐策略 C（stratified by listing age 的 time-future hold-out）
  → 划分一旦锁定，全程不许改动（改了就等于窥探 held-out）
```

#### 2.x 强制区分 In-Sample vs Held-Out 指标（动态宇宙策略必做）

每次实验的 `results.tsv` 必须同时记录两套指标：

| 列 | 含义 | 用途 |
|---|---|---|
| `is_cagr` | In-sample（训练 fold）CAGR | 参考指标，**不**用于 keep/discard 判定 |
| `oos_cagr_median` | Held-out 折的中位数 CAGR | 判定主指标 |
| `oos_cagr_worst` | Held-out 折的最差 CAGR | 红线指标 |
| `generalization_ratio` | oos_median / is_mean | 过拟合检测 |

**核心原则**：任何冠军判定、阶段切换、里程碑 tag 都**只看 oos_*** 指标。
`is_*` 指标只用于诊断"为什么没泛化"，不用于"这个版本比上个版本好"的对比。

**如果 results.tsv 里还没有这些列**：优先级 P0，在做任何新实验前先补上。
旧实验可以回填（重跑 fold-level 评估），实在回填不了就标记为"pre-k-fold era"
并从冠军候选池中剔除。这是一次痛苦但必要的重置。

**心智模型切换**：量化回测的心智模型必须从"历史数据的完整回放"切换到
"ML 模型训练"。回测的时间范围和币种范围都是**训练样本**，不是完整样本。
"全历史 CAGR 3000%" 从"回放"框架看是一个结论，从"训练"框架看是一个
in-sample 指标，在 held-out 验证之前几乎不可信。详见核心原则第 7 条。

### 3. 创建实验分支
```bash
git checkout -b research/<tag>   # 如 research/mar26-chop-opt
```

### 4. 初始化 results.tsv
```
创建 results.tsv，只有 header，baseline 在第一次运行后填入：

commit	sharpe	cagr	maxdd	box1	box2	box3	score	status	hypothesis	conclusion
```

### 5. 运行 baseline
第一次实验永远是跑当前代码作为 baseline。所有后续实验都和 baseline 对比。

## 评估框架

### 复合评分公式

不能只看一个指标。使用加权复合评分，**权重根据当前研究阶段动态调整**
（详见"研究阶段策略"章节）：

```python
# 默认权重（Phase 1 增长期）
score = (
    0.30 * normalize(sharpe) +
    0.40 * normalize(cagr) +         # Phase 1 重 CAGR
    0.15 * normalize(-maxdd) +
    0.15 * normalize(regime_consistency)
)

# Phase 2（防御期）：0.35 sharpe + 0.20 cagr + 0.30 maxdd + 0.15 consistency
# Phase 3（打磨期）：0.30 sharpe + 0.20 cagr + 0.20 maxdd + 0.30 consistency

# regime_consistency = min(box1, box2, box3) / max(box1, box2, box3)
# 越接近 1 说明策略在不同市况下表现越均衡
```

### 红线守卫（violation = 立即 discard）

```
硬红线（任一违反即 discard，不管 score 多高）：
- MaxDD > baseline_maxdd * 1.2      （回撤不能比 baseline 恶化超过 20%）
- 任意 regime 收益 < baseline * 0.7  （单期不能比 baseline 差超过 30%）
- 交易次数 < baseline * 0.5          （策略不能退化为"少交易"获得假Sharpe）
- 回测报错或崩溃
```

### 判定标准

```
keep    — score 提升 >= 0.5% 且无红线违反
          或 score 持平但代码更简洁（简洁性奖励，同 autoresearch）
discard — score 下降，或触及红线
trade-off — score 某些维度提升、某些下降（记录但不自动 keep，标记待人工决策）
crash   — 回测报错
```

## 研究阶段策略（三阶段渐进框架）

研究不是漫无目的地优化所有指标。根据策略当前的成熟度，研究重心应该有明确的阶段性聚焦。
每次研究开始时，先判断当前处于哪个阶段，然后按该阶段的优先级生成假设。

### Phase 1：增长期（Growth）— 主攻 CAGR

**进入条件**：CAGR 仍有明显提升空间（距离历史最佳或理论天花板 > 15%）

**核心目标**：最大化绝对收益

**策略**：
- 实验方向集中在信号灵敏度、入场时机、仓位放大、趋势捕捉等
- 此阶段可以容忍稍大的回撤（红线内），换取更高的 CAGR
- 复合评分权重偏移：

```python
# Phase 1 权重：重 CAGR，轻 MaxDD
phase1_score = (
    0.30 * normalize(sharpe) +
    0.40 * normalize(cagr) +         # ← 权重最高
    0.15 * normalize(-maxdd) +       # ← 适度放松
    0.15 * normalize(regime_consistency)
)
```

**退出信号**：连续 10+ 个实验 CAGR 提升 < 1%，或 CAGR 已触达用户设定的天花板值

### Phase 2：防御期（Defense）— 主攻回撤

**进入条件**：Phase 1 退出，CAGR 已在高位但回撤仍然显著

**核心目标**：在尽量不损失非回撤期收益的前提下，减小最大回撤和回撤持续时间

**策略**：
- ⚠️ **先创新后调参！** 优先尝试结构性创新（因子轮动、新信号、条件化选币），
  而不是直接去调杠杆/止损/仓位大小等参数。参见"创新优先于参数调优"章节。
- 创新方向：回撤期因子轮动、动量崩溃风险预测、跨资产信号、质量/流动性因子
- 参数调优方向（在创新方向探索完毕后）：止损逻辑、震荡期仓位缩减、回撤期信号过滤
- **铁律：非回撤期的收益不能显著下降**（< 5% 损失为可接受代价）
- 如果一个改动让 MaxDD 减少 5pp 但 CAGR 掉 3pp，这通常是好 trade-off
- 如果一个改动让 MaxDD 减少 2pp 但 CAGR 掉 8pp，这是坏 trade-off
- **最佳结果**：CAGR 和 MaxDD 同时改善。如果只做参数调优几乎不可能达到，
  但结构性创新经常能做到（实战已验证）
- 复合评分权重偏移：

```python
# Phase 2 权重：重 MaxDD，保 CAGR
phase2_score = (
    0.35 * normalize(sharpe) +
    0.20 * normalize(cagr) +         # ← 保住就好
    0.30 * normalize(-maxdd) +       # ← 权重最高
    0.15 * normalize(regime_consistency)
)
```

**评估技巧**：
- 将权益曲线的回撤段单独标出，对比改动前后的回撤深度和恢复速度
- 关注 Calmar Ratio（CAGR / MaxDD）作为阶段性辅助指标
- 红线调整：CAGR 不能比 Phase 1 最终值低 > 10%

**退出信号**：MaxDD 已低于目标值，或连续 10+ 个实验 MaxDD 改善 < 0.5pp

### Phase 3：打磨期（Polish）— 主攻平台期

**进入条件**：Phase 2 退出，CAGR 高、回撤小，但权益曲线仍有明显"走平"段

**核心目标**：在尽量不损失非平台期收益的前提下，缩短平台期（将横盘转为增长）

**策略**：
- 先识别权益曲线的平台期（连续 N 天收益率 ≈ 0 的区间）
- 分析平台期对应的市况：是震荡？是低波动？是假突破频发？
- 实验方向：针对平台期市况的专门信号（如震荡期网格、低波动期的均值回归）
- **铁律：非平台期（已有正收益的区间）不能显著受损**（< 3% 损失为可接受代价）
- 复合评分权重偏移：

```python
# Phase 3 权重：重 regime 一致性（消除弱 regime = 消除平台期）
phase3_score = (
    0.30 * normalize(sharpe) +
    0.20 * normalize(cagr) +
    0.20 * normalize(-maxdd) +
    0.30 * normalize(regime_consistency)  # ← 权重最高
)
```

**评估技巧**：
- 定义"平台期"指标：max consecutive days with cumulative return < X%
- 对比改动前后各 regime 的收益分布，确保正收益 regime 不被拖累
- 关注 regime_consistency 指标提升（各段表现更均衡 = 平台期被填补）

**退出信号**：权益曲线已接近理想形态（平滑上升），或无法再缩短平台期

### 阶段判定与切换

```
研究开始时的阶段判定流程：

1. 运行 baseline，获取指标
2. 判断阶段：
   - CAGR < 用户目标值 * 0.85        → Phase 1（增长期）
   - CAGR >= 目标值 * 0.85 且 MaxDD > 目标值  → Phase 2（防御期）
   - CAGR 达标 且 MaxDD 达标 且有明显平台期    → Phase 3（打磨期）

3. 在 results.tsv 的 hypothesis 列标注当前阶段：
   [P1] lower entry threshold for more trades
   [P2] add trailing stop in drawdown regime
   [P3] enable mean-reversion in low-vol plateau

4. 阶段切换时，重新计算 score（用新阶段的权重），
   但保留旧阶段的 results 记录供参考
```

**重要**：阶段不是严格线性的。如果 Phase 2 的某个实验意外让 CAGR 大幅跳升，
可以回到 Phase 1 继续挖掘增长空间。灵活判断，但每个实验必须有明确的阶段归属。

## 实验循环

**LOOP FOREVER（直到人工中断）：**

### Step 1：形成假设

在修改代码之前，**必须写出书面假设**，并标注类型：

```
类型：[INNOVATION] / [PARAM-TUNE]
来源：Innovation Backlog #3 / Deep Dive 发现 / 上一实验衍生 / 近失回收
阶段：[P1] / [P2] / [P3]

假设：引入 Amihud 非流动性因子作为 bear regime 的选币过滤器
预期效果：
- Bear 期：正面——过滤掉低流动性币种，减少滑点和假信号
- Bull/Chop 期：中性——高流动性币本就是动量策略的主力标的
- 综合预期：MaxDD 改善 3-5pp，CAGR 轻微下降 1-2pp（可接受的 trade-off）
依据：Bear 市低流动性币波动更剧烈且流动性骤降，是回撤的主要来源
创新点：改变了 bear regime 的选币逻辑，不只是调仓位大小
```

**假设质量检查**（写完后自问）：
- 这个改动会让策略在不同的时间做出不同的决策吗？→ 是 = 创新，否 = 调参
- 如果标注了 [INNOVATION] 但答案是"否"，诚实改标为 [PARAM-TUNE]
- 如果连续 3 个假设都是 [PARAM-TUNE]，停下来问：
  "Innovation Backlog 里有没有该做的创新？为什么在回避？"

为什么要写假设：
- 防止盲目调参（"试试看把这个改成 0.3"不是假设）
- 实验结果和预期对比时能学到东西
- 如果假设频繁和结果不符，说明你对策略的心智模型有误，需要停下来重新理解
- **强制区分创新和调参，保持科研的主动性**

### Step 2：修改代码

只修改约定范围内的文件。改动要小且聚焦——**每次实验只改一个变量**。

```
好的实验设计：
✅ 只改 chop_floor: 0.45 → 0.40（单变量）
✅ 只加一个新模块（如 V32 smart boost），其他不动
✅ 只删一个模块，看 score 是否不变（简洁性测试）

坏的实验设计：
❌ 同时改 chop_floor + safe_cap + decline_mult（多变量，无法归因）
❌ "大幅重构策略"（无法和 baseline 对比）
```

### Step 3：git commit

```bash
git commit -m "exp: [假设摘要]"
# 如: git commit -m "exp: lower chop_floor 0.45→0.40 for more chop trades"
```

### Step 4：运行回测

```bash
# 将输出重定向到文件，不要让输出淹没 context
[backtest command] > run.log 2>&1
```

读取关键指标：
```bash
# 根据项目具体的输出格式提取（这里是示例）
grep -E "sharpe|cagr|maxdd|box" run.log
```

### Step 5：评估结果

1. 提取所有指标
2. 检查红线（任一违反 → 立即 discard）
3. 计算 composite score
4. 与 baseline（或当前最佳）对比
5. 对比假设和实际结果——**写出结论**

### Step 6：记录到 results.tsv

```
commit	sharpe	cagr	maxdd	box1	box2	box3	score	status	hypothesis	conclusion
a1b2c3d	1.762	130.4	28.1	+133.6	+45.2	-23.0	1.000	keep	baseline	baseline established
b2c3d4e	1.876	144.3	29.5	+159.8	+42.1	-20.8	1.065	keep	lower chop_floor 0.45→0.40	hypothesis confirmed: Box1 +26pp, Box3 +2pp, slight MaxDD increase acceptable
c3d4e5f	1.821	138.7	30.2	+148.3	+40.5	-22.1	1.031	discard	add V35 vol adaptive	worse than b2c3d4e, V35 conflicts with V32 as suspected
d4e5f6g	0.000	0.0	0.0	0	0	0	0.000	crash	double model width	OOM during backtest
```

**注意：results.tsv 不要 git commit，留在 untracked 状态**（同 autoresearch）。

### Step 7：keep 或 discard

```
如果 keep：保留 commit，继续在此基础上迭代
如果 discard：git reset --hard HEAD~1，回到上一个 keep 点
如果 trade-off：保留 commit 但在 tsv 标记 trade-off，继续用上一个 keep 点迭代
如果 crash：修 bug 重试一次，还是 crash 就放弃，git reset
```

### Step 8：回到 Step 1（带科研主动性要求）

**生成下一个假设时，必须按以下优先级顺序选择，不能跳级：**

```
优先级 1（最高）：Innovation Backlog 中未尝试的结构性创新
  → 先看 backlog，里面一定有你还没试的创新方向
  → 如果 backlog 为空，立即触发"Deep Dive 深挖"（见下方）补充

优先级 2：上一个实验的结论衍生出的新创新方向
  → 一个失败的创新实验也有价值——它暴露了哪些假设是错的？
  → 问："如果换一种方式实现同一个创新思路呢？"
  → 一个创新方向至少尝试 3 种不同的实现方式再放弃

优先级 3：之前 discard 的实验中接近成功的"近失"回收
  → 在新的代码基础上重试之前差一点成功的实验

优先级 4：简洁性测试 / 阶段切换检查

优先级 5（最低）：参数调优
  → 只在优先级 1-4 全部穷尽后才做
  → 标注 [PARAM-TUNE]，保持比例 < 40%
```

**各阶段的创新方向指引（不是完整列表——你应该发掘更多）：**

```
Phase 1（增长期）— 创新方向：
  ✅ 引入新因子类型（均值回归、量价关系、链上数据、跨资产信号）
  ✅ 改变因子组合方式（线性加权 → 非线性、排序 → 分位数、固定 → 自适应）
  ✅ 改变选币逻辑（top-N → 条件过滤、等权 → 信号强度加权）
  ✅ 改变入场/出场信号结构（连续 → 离散、阈值 → 突破确认、单一 → 多重确认）
  ⚠️ 信号灵敏度调整、仓位放大 = 参数调优，不是创新

Phase 2（防御期）— 创新方向：
  ✅ 回撤期因子轮动（不同市况用不同因子组合）
  ✅ 动量崩溃风险预测信号（新的风险因子）
  ✅ 跨资产风险信号（BTC 波动率、资金费率异常、相关性突变）
  ✅ 自适应 regime 切换（不是人工阈值，而是数据驱动的检测）
  ⚠️ 调杠杆、加止损、回撤减仓 = 参数调优，不是创新

Phase 3（打磨期）— 创新方向：
  ✅ 平台期专门信号（低波动期均值回归、震荡期网格逻辑）
  ✅ 时间周期自适应（不同市况用不同的 K 线周期）
  ✅ 仓位信号化（仓位大小由信号强度决定，而非固定比例）
  ⚠️ 调整现有参数的数值 = 参数调优，不是创新
```

### Deep Dive 深挖（Innovation Backlog 补充机制）

**当 Innovation Backlog 为空或感觉"想法用完了"时，不是停止的理由，
而是需要更深入地挖掘。**

```
Deep Dive 流程（每次耗时 10-15 分钟，不跑回测，纯思考+阅读）：

1. 重读策略代码
   → 逐行阅读核心策略文件，不是扫一眼
   → 关注：被注释掉的代码、TODO 标记、已禁用的功能
   → 问自己："这段代码为什么这样写？有没有完全不同的写法？"

2. 审视权益曲线
   → 找到权益曲线上每一个"意外"：
     - 大跌之前发生了什么？策略有信号但没行动？
     - 大涨期间策略是满仓还是轻仓？为什么？
     - 平台期期间策略在做什么？是不交易还是交易但亏赢相抵？
   → 每个"意外"都可能是一个创新假设的起点

3. 逆向思维
   → "如果我要故意让这个策略变差，我会怎么改？"
   → 答案的反面可能就是改进方向
   → "如果这个策略在 2027 年彻底失效了，最可能的原因是什么？"
   → 答案就是你应该去解决的脆弱性

4. 跨领域灵感
   → 其他策略类型的思路能不能借用？
     - 做市策略的库存管理 → 仓位管理的新思路
     - 套利策略的价差检测 → 新的入场信号
     - 期权策略的波动率建模 → 新的风险因子
   → 传统金融的方法能不能用到加密市场？
     - Fama-French 因子、动量生命周期、流动性溢价

5. 补充 Innovation Backlog
   → Deep Dive 结束后必须至少补充 5 个新的创新假设
   → 如果想不出 5 个，说明你对策略的理解还不够深——继续读代码
```

**Deep Dive 触发时机**：
- Innovation Backlog 见底时（强制触发）
- 每累计 15 个实验后（定期触发，即使 backlog 还有内容）
- 连续 3 个创新实验都 discard 后（可能方向不对，需要重新思考）

### Innovation Backlog 管理

在研究过程中持续维护一个创新想法清单（保存在 research plan 中）：

```
Innovation Backlog（示例）：
□ [P1] 引入 Amihud 非流动性因子作为选币过滤器
□ [P1] 尝试因子得分的非线性组合（如 RankIC 加权）
□ [P2] 构建动量崩溃预警信号（基于 momentum reversal 速度）
□ [P2] 利用 BTC dominance 变化作为 regime 切换信号
□ [P3] 平台期自动切换到 mean-reversion 子策略
□ [P3] 引入交易量 profile 检测假突破
✅ [P1] 因子轮动（已完成，keep）
✗ [P2] 波动率 targeting（已完成，discard — 与现有模块冲突）

规则：
- 新想法随时添加，不要"等到合适的时候"
- 完成的标 ✅（keep）或 ✗（discard + 原因）
- Backlog 中至少保持 5 个未尝试的想法
- 低于 5 个 → 触发 Deep Dive
```

## 启动协议

**Alpha Lab 必须由用户手动启动，不能自动触发。**

启动时执行以下协议：

### Step 0：启动前问答（一次问完，不要分多轮）

**科研过程是无人值守的，所有需要用户确认的事项必须在启动前全部问清。**

启动前向用户确认以下所有信息（一次性提问，附默认值供用户快速确认）：

```
必问项（每次都问）：

1. 研究时间预算
   "本次研究的最长时间预算是多少？"
   默认：4 小时
   记录为 max_research_time

2. 回测周期
   "回测覆盖多长的历史数据？"
   默认：近 4 年
   记录为 backtest_period

3. 目标 CAGR
   "目标年化收益率（CAGR）是多少？用于判断 Phase 1 是否到达天花板。"
   默认：200%
   记录为 target_cagr

4. 目标 MaxDD
   "可接受的最大回撤上限是多少？用于 Phase 2 判定和红线设置。"
   默认：25%
   记录为 target_maxdd

5. 研究范围
   "本次研究的重点方向或约束？（如：只优化震荡期、不动 XX 模块、重点测试 XX 功能）"
   默认：无约束，全方位优化

6. 回测命令与文件
   "确认回测命令和可修改的文件范围？"
   （如果 session-memory 中有上次的配置，展示给用户确认即可）
```

```
视情况追问（根据项目实际情况判断是否需要）：

7. Walk-Forward 验证方式
   "防过拟合验证选哪种方式？
    方式一（推荐）：保留最后 N 个月做 OOS，日常实验只用前面的数据
    方式二（更严格）：滚动 Walk-Forward，每个窗口都验证
   默认：方式一，保留最后 9 个月
   记录为 oos_months

8. Regime 分段定义
   如果用户没有预定义 regime，需要讨论如何分段

9. 额外红线
   用户是否有特殊约束（如：交易频率上限、单笔最大仓位等）

10. 偏好的实验方向
   用户是否有特别想尝试或特别想避免的方向
```

### Step 1：申请权限与环境检查

**科研期间无人值守，所有权限必须提前获取，不能中途卡住等用户。**

```
权限检查清单：

□ 文件读写权限
  → 确认策略代码、配置文件、回测引擎所在目录可读写
  → 确认 results/ 目录可写（用于保存 dashboard）

□ Git 权限
  → 确认可以创建分支、commit、tag
  → 创建研究分支：git checkout -b research/<tag>
  → 测试一次 git commit 是否正常工作（处理可能的 lock 文件问题）

□ 回测命令可运行
  → 先跑一次回测确认命令正确、输出格式可解析
  → 记录回测耗时，用于更精确的产能估算

□ Dashboard 生成环境
  → 确认 comparison-dashboard skill 可用

□ 网络 / API（如果回测需要）
  → 确认回测不依赖实时数据，或相关 API 可访问
```

如果任何权限或环境检查失败，**在启动前就解决**，不要带着问题进入无人值守循环。

### Step 2：确认启动并报告预估

```
所有信息确认后，向用户汇报：

"启动参数确认：
 - 时间预算：X 小时
 - 回测周期：近 X 年（YYYY-MM ~ YYYY-MM）
 - 目标 CAGR：X%  |  目标 MaxDD：X%
 - 研究范围：[描述]
 - 预估产能：约 XX-XX 个实验

 环境检查全部通过 ✅ 现在开始研究。"
```

### 启动后：自主运行，绝不中断询问

一旦启动，**不要暂停询问用户是否继续**。用户可能在睡觉。
你是自主研究员。如果想法用完了，重新读代码、读之前的实验结果寻找灵感、
尝试更大胆的改动。

**唯一允许中断用户的情况**：回测引擎崩溃且无法自行修复（如数据文件损坏、
依赖服务挂了）。此时输出已完成的成果，说明中断原因，等待用户介入。

## 终止条件

研究停止**只有两种情况**，没有第三种：

### 终止条件 1：找到全局最优

判定标准（**全部**满足才能宣布，不是"至少 3 条"）：
```
□ Innovation Backlog 清空：所有记录的创新想法都已尝试或有明确的排除理由
□ Deep Dive 至少做过 3 次：且最后一次 Deep Dive 未能产出有效的新方向
□ 结构性创新已覆盖至少 5 个不同的维度：
  （如：新因子、因子组合方式、选币逻辑、regime 切换、风控信号）
  → 不是 5 个实验，是 5 个维度，每个维度可能有多个实验
□ 每个创新维度至少尝试过 2 种不同的实现方式再放弃
□ 连续 15+ 个实验 score 提升 < 0.2%（含创新实验，不能只是参数调优凑数）
□ 已触发过至少 2 次"跳出局部最优"机制且均未找到更好的解
□ 简洁性审计通过：删除任何模块都会导致 score 下降
□ 创新比例达标：整个研究过程中 [INNOVATION] >= 60%
```

**穷尽创新审计**（在宣布全局最优前必须完成）：
```
回答以下每个问题，如果任一回答"否"→ 还不能宣布全局最优：

1. "我是否尝试过至少 2 种完全不同的因子类型？"
   （不是调参数，而是引入新的 alpha 来源）

2. "我是否尝试过改变因子的组合/加权方式？"
   （线性 → 非线性、等权 → 信号强度加权等）

3. "我是否尝试过改变选币/入场逻辑的结构？"
   （top-N → 条件过滤、固定阈值 → 自适应阈值等）

4. "我是否尝试过利用跨资产/跨周期信息？"
   （BTC 信号、多周期融合、链上数据等）

5. "我是否深入分析过权益曲线上每个'意外'并针对性设计实验？"
   （大跌、平台期、错过的大涨——每个都试过改善了吗？）

6. "对于每个失败的创新方向，我是否至少尝试了 2 种不同的实现方式？"
   （一种实现失败不代表方向错误，可能只是实现方式不对）

7. "我最后一次 Deep Dive 是什么时候？距现在超过 10 个实验了吗？"
   （如果超过了，再做一次 Deep Dive 再决定）
```

达到此状态后，**先执行"机制 9：最终上线验证协议"**。
只有通过 Final Gate（⭐⭐ 以上）才能宣布全局最优。
如果 Final Gate 未通过（⭐），说明找到的是过拟合的假最优，
需要根据失败项回溯修正（如降低参数敏感度、处理 decay 问题），然后继续研究。

通过 Final Gate 后，执行"研究终止交付流程"。

### 终止条件 2：到达最长研究时间

```
当 elapsed_time >= max_research_time 时：

⚠️ 不要立即中断！必须完成当前迭代的完整流程：
1. 如果正在跑回测 → 等回测完成，评估，记录
2. 如果刚发现了一个 keep → 触发里程碑检查点（dashboard + review + commit）
3. 如果正在修 bug → 修完再停

完成当前迭代后，执行"研究终止交付流程"（见下方），
标注"因时间预算用尽而停止"，并列出"如果有更多时间，下一步会尝试什么"。
```

**注意**：除以上两种情况外，**绝对不要**因为"感觉差不多了"而自行停止。
用户给了时间预算就要用满，除非已确认全局最优。

**永不言弃的铁律**：
- 时间预算没到 + 目标没达到 = 继续研究，没有例外
- "所有想法都试完了" 不是停止的理由——做 Deep Dive，逼自己想出新的
- "连续 N 次失败" 不是停止的理由——这说明需要更大胆的创新，而非放弃
- "结果已经比 baseline 好很多了" 不是停止的理由——好很多 ≠ 全局最优
- 只有"穷尽创新审计 7 个问题全部回答'是' + Final Gate 通过"才是合法的终止

## 防过拟合与 Decay 防御体系

这是量化版和 autoresearch 最大的区别。ML 训练有验证集天然防过拟合，
但量化回测没有——你优化的就是历史数据本身。更危险的是：做了 100 个实验后，
即使策略没有真正的 alpha，纯靠随机也能找到几个"看起来不错"的结果（多重检验问题）。

**本节的目标：确保科研找到的"全局最优"是真正经得起实盘检验的好策略，
而不是一个精心拟合历史数据的幻觉。**

---

### 机制 1：Walk-Forward 滚动验证（核心防线）

**这是对抗过拟合最强力的武器。**

```
原理：
  不要在全量数据上评估策略。将回测数据切分为"训练窗口"和"验证窗口"，
  滚动推进，模拟"用过去的数据做决策，在未来的数据上检验"。

实施方式：
  假设回测数据：2022-03 ~ 2026-03（4 年）

  方式一：固定 OOS（Out-of-Sample）保留区
  ┌──────────────────────────────┬──────────┐
  │  训练集 2022-03 ~ 2025-06   │  OOS     │
  │  （日常实验都在这个范围跑）   │2025-06~  │
  │                              │2026-03   │
  └──────────────────────────────┴──────────┘
  → 最后 9 个月的数据完全不碰，只在最终验证时使用一次
  → 日常实验的 score 只基于训练集计算
  → OOS 是"信封里的答案"——打开一次就失去效力

  方式二：滚动 Walk-Forward（更严格）
  Window 1: 训练 2022-03~2023-09 → 验证 2023-09~2024-03
  Window 2: 训练 2022-03~2024-03 → 验证 2024-03~2024-09
  Window 3: 训练 2022-03~2024-09 → 验证 2024-09~2025-03
  Window 4: 训练 2022-03~2025-03 → 验证 2025-03~2025-09
  → 策略必须在每个验证窗口都盈利
  → Walk-Forward Efficiency = mean(OOS_return) / mean(IS_return)
  → WFE > 0.5 说明策略有真正的预测力；WFE < 0.3 = 严重过拟合嫌疑

启动时决定：
  在 Setup Step 0 中询问用户选择哪种方式（推荐方式一，简单有效）。
  如果回测数据 < 3 年，只能用方式一（保留最后 20% 做 OOS）。
```

### 机制 2：多重检验修正（实验次数越多越要怀疑）

```
问题：
  做了 100 个实验，最好的那个 score 提升了 8%。这是真 alpha 还是随机运气？
  如果扔 100 次硬币，最长连续正面可以轻松到 6-7 次。同理，100 个实验中
  出现一个"看起来很好"的结果，可能纯粹是运气。

应对方法：

  1. 跟踪"好运指标"（Luck Ratio）：
     luck_ratio = best_score_improvement / median_score_improvement
     → 如果 best 比 median 好 5 倍以上，要高度怀疑这是统计噪声
     → 真正的好改动应该在中位数以上但不会离谱地高

  2. 重复验证：
     对每个 keep 的实验，换一组稍微不同的参数（±5%）重跑一次。
     如果换参数后 score 显著下降 → 这是参数敏感型过拟合，不是真 alpha

  3. 心理锚点：
     每 20 个实验后问自己：
     "如果从零开始，只用当前的理解和策略架构，不做任何参数调优，
      score 会是多少？"
     → 这个"朴素 score"和优化后 score 的差距 = 过拟合的上限估计
```

### 机制 3：参数稳定性检验（Parameter Sensitivity）

```
核心原则：好策略应该是"参数不敏感"的——稍微变动参数不应导致表现崩塌。

检查方法（每个里程碑版本必做）：

  1. 识别所有关键参数（阈值、窗口长度、仓位系数等）
  2. 对每个参数做 ±10%、±20% 的扰动
  3. 重跑回测，记录 score 变化

  判定标准：
  ✅ 稳健：所有参数 ±20% 后 score 波动 < 15% → 真正的好策略
  🟡 可接受：大部分参数 ±20% 后 score 波动 < 25% → 轻微过拟合风险
  🔴 脆弱：存在参数 ±10% 后 score 暴跌 > 30% → 严重过拟合

  如果发现脆弱参数：
  → 该参数的当前值可能恰好"踩中"了历史数据的某个噪声特征
  → 应主动将参数移到"稳健区间"的中心（即使 score 略低）
  → 一个稍低 score 但参数稳健的策略 >> 高 score 但参数脆弱的策略

  简化版（快速执行）：
  → 不需要逐个参数扫描，可以直接做"联合扰动"：
     所有参数同时随机 ±10%，跑 5 次，看 score 分布的标准差
  → std(score) / mean(score) < 0.1 → 稳健
  → std(score) / mean(score) > 0.25 → 脆弱
```

### 机制 4：Performance Decay 检测（因子/信号衰减）

```
核心问题：策略在回测前半段表现好、后半段变差，是最常见的过拟合症状。
它也是实盘可能遇到的最大风险——你上线的那一刻，可能就是策略"后半段"的开始。

检测方法：

  1. 时间分段对比：
     将回测期等分为 2-4 段，分别计算每段的关键指标：

     Half 1 (2022-03 ~ 2024-03):  CAGR=180%  Sharpe=2.1  MaxDD=15%
     Half 2 (2024-03 ~ 2026-03):  CAGR=120%  Sharpe=1.4  MaxDD=22%
     → Decay Ratio = Half2_CAGR / Half1_CAGR = 0.67 → 🟡 有衰减趋势

     判定：
     Decay Ratio > 0.85 → ✅ 稳定，衰减可忽略
     Decay Ratio 0.6~0.85 → 🟡 有衰减，需关注但可接受
     Decay Ratio < 0.6 → 🔴 严重衰减，策略可能已经过时或过拟合

  2. 滚动窗口 Sharpe 趋势：
     用 6 个月滚动窗口计算 rolling_sharpe，画趋势图。
     → 如果 rolling_sharpe 呈下降趋势 → 信号在衰减
     → 如果 rolling_sharpe 围绕均值波动 → 正常的周期性
     → 对 rolling_sharpe 做线性回归，斜率 < 0 = 衰减信号

  3. 最近期表现权重：
     在最终评估中，给最近 1 年的表现更高的权重：
     adjusted_score = 0.3 * score_early + 0.7 * score_recent
     → 近期表现差的策略即使总分高，也不适合上实盘

  4. 因子贡献度变化：
     如果策略有多个信号/因子，检查各因子的贡献度是否随时间变化。
     → 某个因子在前两年贡献了 60% 的 alpha，后两年只有 10% → 该因子在衰减
     → 这比整体 CAGR 衰减更有诊断价值——你知道具体是哪个信号在失效
```

### 机制 5：Regime 分段验证

不接受"只在一个 regime 好的改动"。每次评估都看所有 regime 的表现。
`regime_consistency` 指标确保策略在不同市况下都 work。

### 机制 6：交易次数监控

Sharpe 可以通过减少交易（只做高确定性交易）虚假提升。
红线要求交易次数不能大幅低于 baseline。

### 机制 7：简洁性偏好与复杂度税

同 autoresearch：**删代码得到相同结果 = 好结果**。
越简洁的策略越不容易过拟合。每 10 个实验做一次"简洁性审计"：
尝试删除最近加入的功能，看 score 是否不变。

**复杂度税（Complexity Tax）**：

```
每个策略增加的"可调参数"都在消耗自由度。用复杂度税抑制过拟合：

complexity_penalty = 0.005 * num_new_params
adjusted_score = raw_score - complexity_penalty

例：baseline 有 8 个参数。添加一个新模块引入 3 个参数：
→ 额外扣 0.015 → 新模块需要带来 > 1.5% 的 score 提升才能覆盖复杂度税

为什么这有效：
过拟合的本质是"用更多参数去拟合有限的历史数据"。
复杂度税让"加参数"的门槛更高，迫使改进来自真正的结构性洞察。
```

### 机制 8：跳出局部最优（全局搜索意识）

**核心理念：局部最优 ≠ 全局最优。不要一条路走到黑。**

当出现以下信号时，说明你可能陷入了局部最优：
```
触发信号（任一即触发）：
- 连续 8+ 个实验 score 提升 < 0.3%（在同一方向上微调）
- 当前阶段的所有"显而易见"的假设都试过了
- 最近 5 个实验全部 discard
- 假设和结果频繁不符（心智模型可能有误）
```

**跳出策略（按激进程度递增尝试）：**

```
Level 1 — 换方向（低风险）：
  停止当前方向的微调，切换到完全不同的优化维度。
  例：一直在调信号参数 → 转去优化仓位管理逻辑

Level 2 — 回退重来（中风险）：
  git reset 回到上一个里程碑版本（不是上一个 keep，而是更早的稳定点），
  从那个点出发走一条完全不同的路。
  例：回退到 baseline，尝试一个全新的模块组合

Level 3 — 结构性重构（高风险）：
  挑战当前策略的基本假设。尝试：
  - 删除被认为"必要"的核心模块，看 score 会怎样
  - 替换整个信号生成逻辑（如从趋势跟踪改为均值回归）
  - 合并/拆分现有模块
  注意：结构性重构前必须 commit + tag 当前最佳版本作为安全回退点

Level 4 — 反向实验（探索性）：
  故意做一个"应该会变差"的改动，观察实际结果。
  如果结果出乎意料地好，说明之前的心智模型有盲区，
  这个发现比 score 本身更有价值。
```

**跳出后的记录**：在 results.tsv 的 hypothesis 列标注 `[ESCAPE-L1/2/3/4]`，
方便回顾哪些跳出尝试成功了。

**关键心态**：宁可花 5 个实验探索一条全新的路（即使最终 discard），
也不要花 15 个实验在一个已经榨干的方向上挤出 0.1% 的提升。
目标是全局最优，不是当前方向的局部最优。

### 机制 9：Cross-Sectional Coin-Fold OOS Validation（跨币外样本验证）🔴 动态宇宙策略必须

**这是时间维度 walk-forward（机制 1）正交的第二道防线。**

```
核心洞察：

  时间维度的过拟合（机制 1 防御）：
    → 策略在历史某段时间上过拟合，新时间段上崩盘
    → 防线：训练 2022-2025，测试 2025-2026

  横截面维度的过拟合（机制 9 防御）：
    → 策略在历史某批币上过拟合，新币种上崩盘
    → 防线：训练在 coin fold A-D，测试 coin fold E

这两种过拟合是独立的。一个策略可以同时：
  (a) 通过 walk-forward（时间 OOS 全绿）
  (b) 崩在 cross-sectional OOS（换一批币就废）

真实案例（V15 DCA, 2026-04-07，详见下方失败路径案例库）：
  在 111 个币 × 4 年数据上做完整的 walk-forward 验证并通过所有时间维度
  的检验，冠军 CAGR 3314%、MaxDD -34.1%、Calmar 97.3。实盘部署在 530
  个币的动态宇宙上，30 天内 -53.7%。事后分析：100% 的已实现亏损集中
  在 set(live) - set(backtest_coins) 差集内，overlap 集合 PnL = $0。
  冠军参数对"从未在训练中见过的币"是完全未定义行为。

  这不是走漏了一个细节——这是 alpha-lab 的一整个维度缺失了。
  机制 1-8 全部是时间维度的防御，没有任何一个检验"换一批币还 work 吗"。
```

**触发条件**：以下任一条满足，必须启用跨币 k-fold：
- 策略是动态宇宙（top-N / rank-based，实盘会自动选币）
- 回测 universe > 50 个币（< 50 统计 power 不够，需要先扩样本）
- 实盘宇宙 ≥ 回测宇宙的 1.5 倍
- 策略参数数量 ≥ 5（即有过拟合风险）

**实施协议**：

```
Step 1 — Fold 划分（在 Setup Step 2 评估框架里就决定，不是事后补）：

  策略 A：随机 k-fold（k=5 推荐）
    universe = load_all_symbols()  # e.g. 530 coins
    np.random.seed(42)  # 锁定种子，结果可复现
    folds = np.array_split(np.random.permutation(universe), 5)

  策略 B：Stratified k-fold（更好，推荐用这个）
    按以下维度分层，保证每 fold 的币分布一致：
    - Listing age bucket（< 180d / 180-720d / > 720d）
    - Volatility tier（low/mid/high，用历史 ATR 分档）
    - Market cap tier（如能拿到）
    → 每 fold 内部是上述 buckets 的按比例采样

  策略 C：Time-stratified（最严格，动态宇宙必备）
    按币的"上线时间"分 fold。例：
      fold_0 = 2020 之前上线的币（"老古董"）
      fold_1 = 2020-2022 上线的币
      fold_2 = 2022-2023 上线的币
      fold_3 = 2023-2024 上线的币
      fold_4 = 2024 之后上线的币（"新上线"）

    然后做 leave-one-future-out：
      用 fold 0-3 训练/搜冠军，在 fold 4 上测试
    → 这最贴近实盘的真实场景（新币在未来持续到来）

Step 2 — 冠军搜索必须在 fold 分离下进行：

  for candidate_params in search_space:
      cv_scores = []
      for held_out_idx in range(5):
          train_universe = union(folds[i] for i in range(5) if i != held_out_idx)
          test_universe  = folds[held_out_idx]

          # 搜索阶段只能看 train_universe
          train_result = run_backtest(params=candidate_params,
                                      universe=train_universe)

          # 评估阶段只看 test_universe（held-out 币）
          test_result = run_backtest(params=candidate_params,
                                     universe=test_universe)

          cv_scores.append({
              'train_cagr': train_result.cagr,
              'test_cagr':  test_result.cagr,  # ← 这是真正关心的
              'test_maxdd': test_result.maxdd,
              'test_calmar': test_result.calmar,
          })

      # 冠军判定指标
      worst_fold_cagr = min(s['test_cagr'] for s in cv_scores)
      median_test_cagr = median(s['test_cagr'] for s in cv_scores)
      worst_fold_maxdd = max(s['test_maxdd'] for s in cv_scores)
      generalization_ratio = median_test_cagr / mean(s['train_cagr'] for s in cv_scores)

Step 3 — 红线（必须加进"红线守卫"章节）：

  🔴 worst_fold_cagr < 0         → discard, 过拟合到特定 fold
  🔴 worst_fold_maxdd > baseline_maxdd × 1.5
                                  → discard, 在某批币上崩盘
  🔴 generalization_ratio < 0.3  → discard, train 和 test 差距太大
  🟡 generalization_ratio < 0.5  → 标记但不立即 discard，
                                    要求深挖为什么 train/test 差距大
  ✅ generalization_ratio > 0.7  → 健康，champion 具备泛化能力

Step 4 — 冠军报告必须呈现 fold-level 数据，不是只有总体：

  报告格式（示例）：

  候选：MEGA_V2_candidate_X
  ┌────────────┬──────────┬──────────┬──────────┬──────────┐
  │ Fold       │ Train    │ Test     │ Test     │ Test     │
  │            │ CAGR     │ CAGR     │ MaxDD    │ Calmar   │
  ├────────────┼──────────┼──────────┼──────────┼──────────┤
  │ 0 (<2020)  │  2800%   │  2100%   │  -42%    │  50.0    │
  │ 1 (20-22)  │  3100%   │  1950%   │  -38%    │  51.3    │
  │ 2 (22-23)  │  2950%   │   180%   │  -71%    │   2.5 🔴 │
  │ 3 (23-24)  │  3200%   │   450%   │  -58%    │   7.8 🔴 │
  │ 4 (>2024)  │  3050%   │  -120%   │  -89%    │  neg  🔴 │
  ├────────────┼──────────┼──────────┼──────────┼──────────┤
  │ Aggregate  │  3020%   │   912%   │  -60%    │  15.2    │
  │ Worst      │          │  -120%   │  -89%    │          │
  └────────────┴──────────┴──────────┴──────────┴──────────┘

  🔴 判定：reject —— fold 2/3/4（新上线币）上完全不 work。
     "Aggregate CAGR 912%" 是在老币上的过拟合收益平均出来的，
     掩盖了新币上的灾难。

  这个表格**必须**生成。"Aggregate CAGR 3020% ✅" 这种汇总结论
  在动态宇宙策略里是不够的，是欺骗性的。
```

**与机制 1（Walk-Forward）的关系**：

```
机制 1 和机制 9 是**正交的两个维度**，必须同时做：

           时间 OOS (机制 1)
                 │
       不通过    │    通过
    ┌───────────┼───────────┐
不  │   双重     │  时间过拟合 │
通  │  过拟合    │ (能泛化到   │
过  │            │ 新币但不能  │
    │            │ 泛化到未来) │
横  │            │            │
截  ├───────────┼───────────┤
面  │ 币种过拟合 │  真正的     │
OOS │ (能泛化到  │  robust    │
    │ 未来但不能 │  alpha     │
(机 │ 泛化到新币)│  ✅ 上线    │
制  │            │            │
9)  │  V15 DCA   │            │
    │ ← 就是这里 │            │
    └───────────┴───────────┘
       通过

V15 DCA 过了机制 1（时间维度没崩），但没过机制 9（换一批币就崩）。
两个防线缺一不可。
```

**实战简化（如果时间紧/算力紧）**：

```
完整 k-fold 很贵。可以分阶段实施：

  Sprint 1（立即可做，低成本）：
    在现有冠军搜索之外，加一个一次性的 hold-out 验证：
    从 universe 里随机 sample 20% 的币作为 permanent holdout，
    绝对不用于任何参数搜索。每次冠军 milestone 都在这 20% 上测试一次。
    如果 milestone CAGR 在 holdout 上掉 > 50% → 过拟合警报。

  Sprint 2（重构冠军搜索）：
    真正的 k-fold 嵌入 champion search pipeline：
    让每组候选参数自动算 fold-level metrics，
    红线判定用 worst_fold 而不是 aggregate。

  Sprint 3（终极防线）：
    Stratified by listing age 的 time-future hold-out（Step 1 的策略 C）
    这最贴近实盘的"新币持续到来"真实场景。

能先做 Sprint 1 就比什么都没有强一个数量级。
```

### 机制 10：最终上线验证协议（The Final Gate）

**在宣布"找到全局最优"之前，必须通过这套最终验证。这是从科研到实盘的闸门。**

```
验证清单（全部通过才能宣布全局最优）：

□ 时间 OOS 验证（机制 1，如果采用了方式一固定保留区）：
  → 用保留的最后 N 个月数据跑一次回测
  → OOS_score / IS_score > 0.6 → ✅ 通过
  → OOS_score / IS_score < 0.4 → 🔴 过拟合确认，不能上线
  → 注意：OOS 数据只能用一次！用完就不再是 OOS 了

□ 🔴 横截面 OOS 验证（机制 9，动态宇宙策略必查）：
  → 在 held-out 币 fold 上（或 20% permanent holdout）跑最终冠军
  → worst_fold_cagr ≥ 0 且 generalization_ratio > 0.5 → ✅ 通过
  → worst_fold_cagr < 0 或 generalization_ratio < 0.3 → 🔴 币种过拟合，不能上线
  → 注意：这是和时间 OOS 正交的另一条防线，两个都必须通过
  → V15 DCA 事故就是在这一关缺位的情况下上线的

□ 参数稳定性检验：
  → 所有参数 ±20% 联合扰动，跑 5 次
  → score 标准差 / 均值 < 0.15 → ✅ 通过

□ Decay 检测：
  → 后半段 vs 前半段 Decay Ratio > 0.7 → ✅ 通过
  → Rolling Sharpe 线性回归斜率 ≥ 0 → ✅ 没有衰减

□ 多重检验意识：
  → 如果总共做了 N 个实验，最终 score 提升了 X%，
    是否有理由相信这不是 N 次试验中的随机最优？
  → "朴素 score"检验：只用当前架构+合理的默认参数（不调优），
    score 是否仍显著优于 baseline？

□ 简洁性确认：
  → 对最终版本再做一次简洁性审计
  → 删除任何一个模块都导致 score 下降 → ✅ 每个模块都有贡献

验证结果在研究总结报告中列出，作为"可上线信心评级"：
  ⭐⭐⭐ 全部通过 → 高信心，推荐上线
  ⭐⭐  4/5 通过 → 中等信心，可上线但需密切监控
  ⭐    ≤3 通过 → 低信心，建议继续优化或重新审视策略架构
```

## 里程碑检查点

当策略达到值得留档的水平时，暂停实验循环，执行完整的检查点流程。

### 触发条件（任一即触发）

```
- score 相比上一个里程碑提升 >= 5%（重大突破）
- 完成了一次阶段切换（如 Phase 1 → Phase 2）
- 成功跳出局部最优，找到了新的更高峰
- 每累计 20 个 keep 实验（定期检查点）
- 即将进行 Level 3 结构性重构前（保存安全回退点）
```

### 检查点流程（必须完整执行，不可跳过）

```
Step 1：触发 comparison-dashboard skill
  → 生成可视化看板，对比当前最佳 vs baseline vs 上一个里程碑
  → 包含权益曲线、分 regime 表现、关键指标变化

Step 2：执行过拟合 & Decay 专项验证（里程碑门禁，不可跳过）

  这一步是里程碑的核心验证环节。目的是在每个重要节点确认策略不是
  "回测幻觉"——只有通过这道门禁的版本才值得继续在其基础上迭代。

  ┌─────────────────────────────────────────────────────────┐
  │ 2a. 参数稳定性检验（Anti-Overfitting）                   │
  ├─────────────────────────────────────────────────────────┤
  │ 方法：                                                   │
  │  1. 识别当前版本所有可调参数（阈值、窗口、系数等）        │
  │  2. 全部参数同时随机 ±10%，跑 3-5 次回测                │
  │  3. 计算 score 的均值和标准差                            │
  │                                                         │
  │ 判定：                                                   │
  │  CV = std(score) / mean(score)                          │
  │  CV < 0.10 → ✅ 稳健（继续）                            │
  │  CV 0.10~0.20 → 🟡 轻微敏感（标记，继续但需关注）       │
  │  CV > 0.20 → 🔴 脆弱（暂停！找出敏感参数并修正）        │
  │                                                         │
  │ 🔴 处理：                                                │
  │  → 逐个参数扰动 ±20%，定位最敏感的参数                  │
  │  → 将敏感参数移到"稳健高原"的中心值（即使 score 略低）   │
  │  → 修正后重新验证，直到 CV < 0.15                       │
  │  → 如果无法修正 → 该里程碑降级，策略有结构性过拟合风险  │
  └─────────────────────────────────────────────────────────┘

  ┌─────────────────────────────────────────────────────────┐
  │ 2b. Performance Decay 检测（Anti-Decay）                 │
  ├─────────────────────────────────────────────────────────┤
  │ 方法：                                                   │
  │  1. 将回测期等分为前后两半，分别计算 CAGR、Sharpe、MaxDD│
  │  2. 计算 Decay Ratio = 后半段_CAGR / 前半段_CAGR        │
  │  3. 用 6 个月滚动窗口计算 rolling Sharpe，做线性回归    │
  │                                                         │
  │ 判定：                                                   │
  │  Decay Ratio > 0.85 → ✅ 稳定                           │
  │  Decay Ratio 0.60~0.85 → 🟡 有衰减，但可接受           │
  │  Decay Ratio < 0.60 → 🔴 严重衰减                      │
  │                                                         │
  │  Rolling Sharpe 斜率 >= 0 → ✅ 无衰减趋势              │
  │  Rolling Sharpe 斜率 < 0 但 > -0.1 → 🟡 轻微衰减       │
  │  Rolling Sharpe 斜率 < -0.1 → 🔴 显著衰减趋势          │
  │                                                         │
  │ 🔴 处理：                                                │
  │  → 定位衰减来源：哪个因子/信号在后期表现变差？           │
  │  → 检查是否是市场 regime 变化导致的自然衰减             │
  │  → 如果是特定因子衰减 → 该因子可能过拟合了早期数据     │
  │  → 考虑引入自适应机制或替换衰减因子                    │
  │  → 如果整体衰减 → 策略可能过度拟合了早期市场结构       │
  └─────────────────────────────────────────────────────────┘

  ┌─────────────────────────────────────────────────────────┐
  │ 2c. Regime 一致性检查                                    │
  ├─────────────────────────────────────────────────────────┤
  │ 方法：                                                   │
  │  1. 分别计算每个 regime（Bull/Chop/Bear）的收益          │
  │  2. 检查是否有 regime 表现显著低于 baseline              │
  │                                                         │
  │ 判定：                                                   │
  │  所有 regime 均优于 baseline → ✅                        │
  │  有 regime 低于 baseline 但在红线内 → 🟡 标记并记录     │
  │  有 regime 严重低于 baseline（< 70%）→ 🔴 触发红线     │
  └─────────────────────────────────────────────────────────┘

  ┌─────────────────────────────────────────────────────────┐
  │ 2d. 里程碑验证报告（必须记录）                           │
  ├─────────────────────────────────────────────────────────┤
  │ 在 results.tsv 的 conclusion 列追加验证结果摘要：        │
  │                                                         │
  │ 格式：[MILESTONE-N] ParamCV=0.08✅ Decay=0.82✅         │
  │       RollingSharpeSlope=+0.02✅ RegimeConsistency=✅   │
  │                                                         │
  │ 如果任一指标为 🔴：                                     │
  │  → 该里程碑标记为 ⚠️ UNSTABLE                          │
  │  → 不作为安全回退点（后续 Level 2 跳出不应回退到此版本）│
  │  → 必须先修正问题再继续迭代                             │
  │  → 修正后重新验证，通过后才升级为正式里程碑             │
  └─────────────────────────────────────────────────────────┘

Step 3：触发 quant-code-review skill
  → 对当前代码做全面审计（含维度 7 供应链安全）
  → 重点关注：实盘/回测对齐、状态持久化、AI 代码质量
  → 发现问题立即修复

Step 4：修复 code review 发现的问题
  → 修复后重新跑一次回测，确认修复没有破坏 score
  → 如果修复导致 score 下降，分析原因（可能审计发现了回测作弊的地方）

Step 5：git commit + tag 留档
  → git tag milestone-<N>-<简短描述>  如 milestone-3-phase2-entry
  → commit message 包含当前所有关键指标 + 过拟合检查结果
  → 这个 tag 是安全回退点，后续实验随时可以回到这里

Step 6：保存 session-memory
  → 记录关键发现和当前状态，供下次会话继续
```

**里程碑版本是神圣的**：一旦 tag 了里程碑，后续实验如果全部失败，
至少还有这个稳定的版本可以回退。里程碑版本必须通过 code review。

## 研究终止交付流程

**终止时必须完成以下全部步骤，将成果完整交付给用户。这是铁律。**

### Step 1：确保最佳版本代码就绪

```
1. git checkout 到表现最好的版本（不一定是最后一个 keep，而是 score 最高的）
2. 如果最佳版本不是最新的里程碑版本，触发 quant-code-review 做一次审计
3. 确认代码干净、可直接用于实盘
4. git tag best-<research-tag>  如 best-mar26-chop-opt
5. 在 commit message 中记录完整指标
```

### Step 2：生成最终可视化 Dashboard

```
触发 comparison-dashboard skill，生成最终看板，必须包含：
- 最佳版本 vs baseline 的权益曲线对比（对数坐标）
- 分 regime 表现对比（柱状图）
- 所有里程碑版本的指标演进趋势
- 关键指标汇总表
- Rolling Sharpe 趋势图（6 个月窗口）— 可视化 decay
- 前后半段表现对比（Decay Ratio）
- Final Gate 验证结果摘要（⭐评级）

Dashboard 文件保存到项目 results/ 目录下，
命名为 research_<tag>_final_dashboard.html
```

### Step 3：生成研究总结报告

```markdown
## 研究总结：[tag]

### 终止原因
[全局最优 / 时间预算用尽]

### 最佳版本
- Git tag: best-<tag>
- Commit: <hash>

### 指标对比
| | Baseline | 最佳（IS） | 最佳（OOS） | 变化 |
|---|---|---|---|---|
| Sharpe | 1.762 | 1.912 | 1.73 | +8.5% (IS) |
| CAGR | 130.4% | 152.1% | 128% | +21.7pp (IS) |
| MaxDD | 28.1% | 29.8% | 26% | +1.7pp (IS) |
| Score | 1.000 | 1.085 | 0.98 | +8.5% (IS) |

### Final Gate 验证结果
| 检验项 | 结果 | 状态 |
|--------|------|------|
| OOS 验证（OOS/IS ratio） | 0.90 | ✅ |
| 参数稳定性（std/mean） | 0.08 | ✅ |
| Decay Ratio（后/前） | 0.82 | ✅ |
| Rolling Sharpe 斜率 | +0.01 | ✅ |
| 简洁性审计 | 全部必要 | ✅ |

**可上线信心评级：⭐⭐⭐**

### 实验统计
- 总实验数：47
- Keep：14 (29.8%) / Discard：28 (59.6%) / Trade-off：3 (6.4%) / Crash：2 (4.3%)
- 里程碑检查点：3 次
- 跳出局部最优：2 次（1 成功 / 1 失败）
- 新增参数数 / 复杂度税累计：+5 / -0.025

### 研究阶段历程
Phase 1（实验 #1-#25）→ Phase 2（实验 #26-#40）→ ...

### 关键发现
1. [最重要的发现]
2. [第二重要的发现]

### Decay 观察
- 前半段 CAGR: ___% | 后半段 CAGR: ___%  | Decay Ratio: ___
- Rolling Sharpe 趋势：[稳定/轻微下降/上升]
- 衰减因子（如有）：[列出衰减显著的信号/因子及诊断]

### 失败路径（避免重复）
1. [尝试过但失败的方向及原因]

### 下一步建议（如因时间用尽而停止）
1. [如果有更多时间，最值得尝试的方向]
```

### Step 4：向用户输出交付物

**必须将以下文件全部交付给用户（使用 present_files）：**

```
交付物清单：
1. 最终 Dashboard HTML          → research_<tag>_final_dashboard.html
2. 研究总结报告                  → research_<tag>_summary.md
3. 完整实验日志                  → results.tsv（原始数据）
4. 最佳版本的策略代码快照        → 确保在 git tag 中，告知用户 tag 名称

输出时的话术模板：
"研究完成！以下是本轮成果：
 - 最佳版本：<tag>（git tag: best-xxx），Score 较 baseline 提升 X%
 - [查看可视化看板](链接)
 - [查看研究报告](链接)
 - [查看实验日志](链接)
 如需回退到最佳版本：git checkout best-xxx"
```

### Step 5：保存 session-memory

将本轮研究的关键发现、最佳版本 tag、失败路径保存到 session-memory，
确保下次会话可以无缝继续。

## 与其他 skill 的协作

- **comparison-dashboard**：里程碑检查点时触发 + 研究结束时触发，生成可视化看板
- **quant-code-review**：里程碑检查点时触发，对当前代码做全面审计，发现问题立即修复
- **session-memory**：里程碑检查点时保存 + 研究结束时保存，确保跨会话上下文不丢失
- **git-cleanup**：研究结束后触发，清理工作区，确保 results.tsv 等临时文件不被误提交

## 经典失败案例库

这一章记录真实发生的研究方法论失败案例。每一条都是用真金白银换来的教训，
里程碑检查点时应该翻一遍，确认本次研究没有踩同样的坑。

---

### 案例 1：V15 DCA 的 Cross-Sectional 过拟合（2026-04-07）

**一句话教训**：时间维度的 walk-forward 全绿，不代表策略能部署。
横截面维度的 k-fold OOS 是独立的第二道防线，缺了就是裸奔。

**症状**：
- Alpha-lab milestone 报告：冠军 MEGA V2 CAGR 3314%, MaxDD -34.1%, Calmar 97.3
- 机制 1-8 全部通过：walk-forward 健康、decay ratio 良好、参数稳定、regime 一致
- 实盘部署 30 天内：$2981 → $1381（-53.7%）

**事后 forensic 归因**：
- 回测 universe：111 个币（`data/historical/` parquet 快照）
- 实盘 universe：530 个币（Binance USDT-M 动态全集）
- 16 笔已平仓亏损（-$1034.20）**100% 集中在 set(live) - set(bt) 差集**
- overlap 集合（回测见过的币）已实现 PnL = $0
- 差集里既有 2-3 年老币（TRU 3y, ONG 2.3y，数据采集缺口导致缺失）
  也有中青年币（4-19 个月新上线），但它们的共同特征是"**冠军搜索时从未见过**"

**为什么机制 1-8 没抓住这个**：

| 机制 | 检验的维度 | 为什么漏掉 |
|---|---|---|
| 机制 1（walk-forward）| 时间 OOS | 在 111 个币上切时间 fold，**币全程固定** |
| 机制 2（多重检验）| 实验次数 luck | 不涉及币种维度 |
| 机制 3（参数稳定性）| 参数 ±10% 扰动 | **币不变** |
| 机制 4（decay ratio）| 前半段 vs 后半段 | **币不变** |
| 机制 5（regime consistency）| BULL/BEAR/RANGE 分段 | **币不变** |
| 机制 6（交易次数）| 交易频率 | 不涉及币种维度 |
| 机制 7（复杂度税）| 参数数量 | 与币无关 |
| 机制 8（跳出局部最优）| 搜索方向 | 与币无关 |

**所有现有防线都默认"币是固定的"，没有任何一个机制检验"换一批币还 work 吗"**。
这是设计盲区，不是执行失误。

**如果当时有机制 9 会发生什么**：
- 把 111 个币按 listing age 分 5 folds
- 冠军候选在 fold 0-3 训练，fold 4 上测试
- 几乎可以肯定：fold 4（新上线币）上 CAGR 会显著低于 fold 0-3，甚至负数
- worst_fold_cagr 红线会直接 discard 这个 candidate
- 真正的冠军会被迫在"对新币也有效"的参数空间里搜索
- 很可能搜不到 CAGR 3314% 这种夸张数字——**但搜到的那个会真的能上线**

**同场景还踩过的其他坑（避免重复）**：

1. **把"让回测见过所有币"当治本方案**
   - 错误框架："让 bt_universe ⊇ live_universe"
   - 为什么错：实盘在时间轴上永远向前跑，新币每天上线，"完全覆盖"是
     一个会永远逃逸的追赶游戏
   - 正确框架：从"覆盖"转向"泛化"——目标不是"回测见过所有币"，而是
     "冠军参数对样本外的币依然有效"

2. **A/B 验证用研究起点做 baseline 而不是生产冠军**
   - 错误：用 H48_LC20_P50（CAGR 374%，alpha-lab 研究起点）做 baseline
     验证 rank normalization
   - 真相：生产冠军是 MEGA V2（CAGR 1967%），baseline 选错直接让验证结论失效
   - 规则：任何 A/B 的 baseline 必须是 live bot 当前实际加载的 profile，
     不是研究阶段中间版本，也不是历史 milestone 截图

3. **把"修 cool bug"当成修根因**
   - 错误行为：花数小时推进 rank normalization（算法层优雅修复），
     跳过 D1（数据样本扩充，工程问题）
   - 教训：数据和评估方法是**上游问题**，算法（新因子、新仓位、修饱和）是
     **下游问题**。上游没修之前，任何下游 A/B 的结论都不可信——你在错误的
     数据分布 + 错误的评估框架上优化

4. **用 allowlist 把实盘锁回 bt 见过的币集合**
   - 错误：V15_PAPER_SAFE 第一版引入 `universe_allowlist_file`
   - 为什么错：这是 band-aid，违背动态宇宙策略的设计哲学
   - 规则：回测的使命是"验证选币能力"，不是"限制交易范围"

**防线修复清单（已写进 skill）**：
1. 机制 9（跨币 k-fold OOS）
2. Setup Step 2 的 in-sample vs held-out 指标区分
3. 最终上线验证协议 The Final Gate 中加入横截面 OOS 关
4. 核心原则第 7 条（回测 = ML 训练）

**元教训**：alpha-lab 的设计偏向"时间序列 ML"范式（walk-forward、decay），
但动态宇宙策略同时也是"横截面 ML"问题（选币 = 对每个币预测期望收益的排序）。
横截面 ML 的标准工具是 cross-sectional k-fold，alpha-lab 之前漏掉了。
**这次事故不是执行层的 bug，是研究方法论框架本身有一个维度盲区**。

---

## Reference

研究模板见 `references/research-template.md`。
每次新研究开始时复制此模板作为研究计划。
