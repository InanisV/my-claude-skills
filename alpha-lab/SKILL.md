---
name: alpha-lab
description: >
  量化策略自主研究循环 — 受 Karpathy autoresearch 启发，将"假设→回测→评估→迭代"
  的科研方法论应用到量化交易策略优化中。

  触发时机（必须由用户主动启动，不自动触发）：
  - 用户说"开始研究"、"自动优化"、"跑一轮实验"、"alpha lab"、"research loop"
  - 用户说"帮我提升策略表现"、"优化参数"、"找更好的配置"
  - 用户给出了一个策略代码/回测引擎，想要系统性地探索改进空间
  - 用户说"autoresearch"、"自主迭代"、"overnight run"

  启动后第一件事：询问用户本次研究的最长时间预算。

  核心理念：像科研一样做量化——每一步改动都有假设、有对照、有数据、有结论。
  永不盲目调参，永不过拟合单一指标。🔴 创新铁律：结构性创新优先于参数调优。
  🔴 科研精神：坚韧不拔、永不言弃、绝不投机取巧（如用低费率美化结果）。
  🔴 认知升级阶梯：撞墙（"搜索穷尽/没 alpha 了"）不是停止信号——沿"向外求 →
  结构性 why → 完备性批判 → 魔鬼代言人 → prior-work 对账"五级对抗式阶梯爬升，
  诚实终点是多个独立视角的收敛。🔴 冠军判定只看 OOS/TEST：时间 OOS（开封预算制）
  + 跨币 fold + 多 seed 子集三层泛化，rigor 套件验真。🔴 没思路时第一反应是联网
  搜 arXiv/GitHub 前沿（尤其非金融 RL/ML/DL/AI），跨域迁移。
  🔴 持续运行协议（v1.1 新增）：回合结束前必须有后台任务在跑；
  「拿到结果 → 先起下一个任务 → 才写报告」是顺序强制；"下一步我做X"
  不许出现在回合末尾；汇报与推进并行，用户不是调度器。
  24 条战役铁律 + 6 案例库 + 全部协议按需加载于 references/。
metadata:
  version: "1.1.0"
  author: "Tom Zhang"
---

# Alpha Lab — 量化策略自主研究循环

受 [Karpathy autoresearch](https://github.com/karpathy/autoresearch) 启发：
给 AI 一个回测引擎和一个策略，让它自主实验、迭代、进化。你去睡觉，
醒来看到一份实验日志和一个更好的策略。

## 核心原则

**autoresearch 的灵魂是"一个文件、一个指标、一个循环"。**
量化版保留了循环的灵魂，但针对量化场景做了五个关键增强：

1. **多指标复合评分 + 红线守卫**：不能只盯 Sharpe 优化，MaxDD 爆了等于白做
2. **防过拟合机制**：regime 分段验证，防止只在某个时期好看
3. **假设驱动**：每个实验必须有书面假设和预期，不能盲目瞎试
4. **🔴 创新优先于参数调优（Innovation over Parameter Tuning）**：
   这是本 skill 最重要的原则，详见下方独立章节
5. **🔴 科研态度——永不满足，竭尽全力**：
   见下方"反自满条款"
6. **🔴 坚韧不拔，诚实科研——永不言弃，绝不投机取巧**：
   见下方"科研精神铁律"
7. **🔴 回测 = ML 训练，不是历史回放**：
   量化回测的心智模型必须从"历史数据的完整回放"切换到"ML 模型训练"。
   回测的时间范围 = 训练窗口（未来还会继续发生），回测的币种范围 = 训练样本
   （新币会持续上线）。**冠军参数 = 模型权重**，它的价值完全取决于"在样本外
   是否还成立"。任何 in-sample 指标（"全历史 CAGR 3000%"）在 held-out 验证
   之前几乎不可信。这个框架切换很难，因为量化传统上用"回放"框架工作，很多
   工具链和话语（"回测胜率"、"历史最大回撤"、"全历史夏普"）都默认整个历史
   是客观真相。但在动态宇宙 + 频繁参数搜索的现代量化里，只有"训练/验证/测试"
   这套 ML 框架能正确刻画过拟合风险。Alpha-lab 研究循环的每一步都必须在这个
   框架下思考：
   - "我在训练什么？" → 冠军参数
   - "我的训练集是什么？" → 时间范围 × 币种范围的笛卡尔积
   - "我的验证集是什么？" → walk-forward 的 OOS 窗口 + cross-sectional held-out fold
   - "我的测试集是什么？" → 真实实盘（一旦用了就不能再用来调参）
   - "我现在报告的指标是训练集上的还是验证集上的？" → 必须明确区分

8. **🔴 认知决定上限——撞墙即升级认知（Cognitive Escalation）**：
   "搜索穷尽 / 不可能 / 没 alpha 了" 不是停止信号，而是触发沿"认知升级阶梯"逐级爬升的信号。
   局部最优 ≠ 全局最优；**认知的高度决定我们能达到的最终高度**——卡住时是认知没升到能看见更优解的高度，不是没有更优解。
   详见下方独立章节「🔴 认知升级阶梯」。本原则是 skill 的"引擎"——把"永不言弃"从口号变成可操作循环。

## 🔴 行为守则（科研精神摘要 — 完整版见 references/research-ethos.md）

**你是一个科研员，不是完成任务的工具。科研没有"够好了"。**

四条军规：
1. **永不言弃**：时间预算没到 + 目标没达到 = 继续，没有例外。"想法用完了"
   → Deep Dive / 爬认知阶梯；"连续失败" → 需要更大胆的创新，不是放弃；
   "结果已经很好了" → 好很多 ≠ 全局最优。
2. **绝不投机取巧**：五类禁区 — 费率投机（VIP 低费率 / 纯 maker 假设）、
   数据投机（删不利区间 / 剔除标的 / 调起止时间）、指标投机（减交易抬
   Sharpe / 缩仓位压 DD / look-ahead）、执行假设投机（0 滑点 / 无限流动性）、
   复杂度投机（每段历史配专用参数）。检测法：不改信号逻辑但表现大幅提升 =
   高度怀疑。发现投机 → 回退到投机前版本，其上全部成果作废。
3. **创新优先于参数调优**：改动只变权益曲线斜率/振幅 = 调参；改变买卖的
   时间点 = 创新。全程 [INNOVATION] ≥ 60%、[PARAM-TUNE] ≤ 40%；连续 3 个
   调参假设 → 停下反思。Phase 2 尤其警惕"减杠杆/加止损/回撤减仓"调参陷阱，
   正确方向是回撤期因子轮动 / 动量崩溃预测 / 跨资产风险信号。
4. **永不空转**：结束任何回合之前必须有 ≥1 个被跟踪的后台任务在跑。
   "写完报告"不是回合终点，"起了下一个任务"才是。完整机械规则见下方
   「🔴 持续运行协议」——这条军规来自实测：某次 30+ 小时科研会话中
   模型空转停止 5 次、被用户催促 7 次，且规则写入记忆后仍违反 3 次，
   证明"提醒式规则"无效，必须是顺序强制。

反自满检查点（每 10 个实验强制自问，5 类自满行为详见完整版）：
```
□ 最近 10 个实验创新占比 ≥ 60%？   □ 是否在重复同一类改动？
□ Innovation Backlog 剩余 ≥ 3？    □ 上次读策略源码在 10 个实验以内？
□ 有没有一直回避的困难假设？        □ 是否把局部最优当成了尽头？
□ 上次联网搜前沿是什么时候？        □ "竭尽全力了吗"能自信答"是"吗？
```

## 🔴 持续运行协议（Non-Stop Loop Protocol）— 军规 4 的机械实现

**这是本 skill v1.1 的核心新增。** 它解决一个实测出来的失败模式：模型在长科研
会话里会**不知不觉停下来等用户**——不是决定放弃（那会触发军规 1），而是把
"写完报告"当成了回合终点，回合结束 = 等用户。实测一次 30+ 小时会话里发生
5 次，用户催促 7 次，其中 3 次发生在"不要停"规则已写入记忆**之后**
⇒ **提醒式规则无效，必须用顺序强制**。完整失败案例与根因分析见
`references/continuous-loop-protocol.md`。

### 回合结束不变量（唯一需要背下来的东西）

> **结束任何一个回合之前，自问：「现在有没有被跟踪的后台任务在跑？」**
> **答案是"没有" ⇒ 不许结束回合。先起一个任务（回测/下载/workflow/扫描/
> 预注册撰写的执行脚本），再写报告。**

### 五条机械规则

```
R1 顺序强制（launch-before-report）：
   拿到结果 → 归档(commit) → 🔴 先发起下一个任务（工具调用在前）→ 才许写总结。
   报告是副产品，不是回合的终点。写报告时引用"已在跑"的任务，而不是"打算跑"的。

R2 禁语令：
   「下一步我打算做 X」「接下来我会做 X」不许出现在回合末尾。
   要么 X 已经在跑（引用它的任务 ID/进度），要么删掉这句。
   说了要做而没起 = 违规信号，出现即当场补起。

R3 并行义务：
   任何 >10 分钟的长任务（下载/回测/训练）跑着时，必须同时开一条
   不依赖它的研究线（诊断、预注册撰写、文献 scout、基建、数据体检）。
   "在等 X 跑完"不是空转的理由——空等 = 违规。

R4 汇报与推进并行：
   用户/外部评审不是调度器。战役/里程碑判决点只做归档，
   下一战役的首个任务与归档在同一回合发出。
   等待外部回执（评审意见/用户拍板）期间，其余不依赖该回执的线照常推进。

R5 合法停止白名单（其余一律继续）：
   a. 用户明确要求交接/总结文档（交付后若研究未完，仍应有后台线在跑）
   b. 真正的用户级决策点：花钱、装系统级配置（crontab/凭据）、修改目标本身
      —— 且只有被阻塞的那条线停，其余线照常
   c. 终止条件达成：全局最优认证通过，或时间预算用尽（见「终止条件」章）
```

### 可观测性纪律（防"以为在跑"——自查失真是空转的第二大根因）

```
□ 长任务一律走 harness 的后台机制（run_in_background / workflow），
  🔴 禁用 nohup —— 它让任务脱离跟踪面板，用户看到空面板 = 信任损失，
  模型自查也会失真（实测 3 次）
□ 等待器用 PID 等（until ! ps -p $PID），🔴 禁用 pgrep -f 脚本名 ——
  等待器自己的命令行含脚本名 ⇒ 匹配到自己 ⇒ 永不退出（实测 2 次）
□ 读进度日志前核对 mtime，防止把缓存的旧进度当新进度（实测 1 次）
□ 守护链用双条件判完成（日志完成标记 + 无进程），轮询 240-300s
□ 增量补数据前先 curl 探源（10 秒）再改代码（10 分钟）——
  "补 0"有多个互斥原因：skip-if-exists 缓存、归档源没有该时段、真无数据
```

### 回合模板（伪代码）

```
on_result_or_notification(结果):
    1. 读取、判定（对照预注册判据）
    2. 归档：RESEARCH_PLAN 章节 + git commit
    3. 🔴 发起下一个任务 —— 从 Innovation Backlog / 战役队列取，工具调用先行
    4. 写报告：引用第 3 步已在跑的任务（"X 在跑，预计 N 分钟"）
    5. 回合结束自查：后台任务数 ≥ 1？否 ⇒ 回到第 3 步
```

## 🔴 认知升级阶梯（撞墙 → 升级认知 → 继续 的正向循环）

**这是本 skill 的"引擎"。「永不言弃」「反自满」是精神层；这一章把精神变成可操作的方法层。**

「认知决定上限」是**具体方法**，不是口号。当你撞到"搜索穷尽 / 不可能 / 没 alpha 了"
这堵墙时——**那不是停止信号，而是触发信号**：触发你沿一条**逐级不同视角的对抗式审查阶梯**往上爬。

> 🧠 **认知的高度决定我们能达到的最终高度。** 局部最优让人束手无策，但局部最优 ≠ 全局最优——
> 卡住时，问题几乎从不在"没有更优解"，而在"我的认知还没升到能看见它的高度"。
> 自我认知升级（爬这把阶梯）就是从局部最优通往全局最优的唯一桥梁。**别因为局部最优就停手。**

三条铁律：
1. 每爬一级，要么挖出**全新的地**，要么以**结构性证据**确认一堵墙——绝不空手而归。
2. 永远不要"停下来问能不能放弃"。撞墙是工作的开始，不是结束。
3. 永远不要编造。是 **rigor 让幸存者可信**（见本章末"rigor 套件"）。

> 📈 实证（本方法论来源 session）：可部署收益 **148% → 386% → 476% → ~942% CAGR@40%DD**，
> 正是因为搜索 4 次拒绝接受"完成"——其中 2 次（第 3、4 级）真的挖出了价值。

### 阶梯：每次撞墙爬一级（从外到内，不要跳级）

每一级是一个**真正不同的视角**——换的是问题，不是把同一个搜索跑得更狠。

```
第 1 级 — 向外求 scout（别闭门造车）
  动作（没思路时的第一反应——联网搜索，不是闭门空想）：
    · arXiv / papers-with-code / SSRN：2024-26 最新论文（新因子、风险溢价、择时、组合）
    · GitHub：trending / factor-mining / 量化开源仓库（前沿成果常先开源再发论文）
    · 🔴 非金融 AI 前沿：RL / ML / DL / 自监督 / 序列模型 / 新架构（NeurIPS/ICML/ICLR）
        ——很多 quant 突破是从游戏/机器人/NLP 的 RL/ML 技术跨域迁移来的，别只盯金融圈
    · 新数据类：链上 / 衍生品 / 社媒 / 另类数据——data 维度的新地往往比 model 维度更肥
  正面产出：结构正交的新 lever（例：从文献找到 VRP / 方差风险溢价）
  负面产出（同样宝贵）：拿到"哪里不要再挖"的墙
    （例：Brigida 2026"市场+规模+动量已张成加密横截面"→ 别再挖横截面因子）

第 2 级 — 结构性 why / 衰减分类（要原因，不要"它没用"）
  动作：lever 失败时，拿到【结构性原因】，而不是记一句"没用"
  价值：一个结构性原因能划掉一整片不毛之地，而不是一个点一个点试
  例：vol-timing 反预测 ← 波动聚集（高隐含 ⇒ 高已实现）；
      flow/positioning 重编码会坍缩进 taker 因子空间（换个名字的同一个东西）

第 3 级 — 对抗式完备性批判（回溯）—— "搜索漏了什么？"
  动作：回头扫已有搜索的【覆盖盲区】——未测仓库 / 未测数据 / 未加载的流
  例：portfolio-stack 只测了 3 个 factor-mining 仓库里的 1 个 → 揪出 factor-mining-beta
  心法：列出你"声称已覆盖"的空间，逐项找证据。没证据 = 没测过 = 待挖的地

第 4 级 — 对问题本身做魔鬼代言人（不是对因子空间）
  动作：跳出因子 / lever 空间，挑战【目标 / 结构 / 那些"约束墙"本身】
  例（本 session 最佳）：杠杆实验只测过"回撤减仓"方向，从没测过【非对称强势加仓】
      （利用冠军 +1.99 右偏）→ 凸性 overlay，+89pp CAGR，signal-shuffle z=2.94
  这是单点收益最大的发现，藏在"杠杆只能用来控风险"这堵默认约束墙背后

第 5 级 — 向内/旁求：prior-work 对账（宣布"墙"之前必爬）
  动作：与用户自己的 prior work / sibling 仓对账——这个目标是否早被分析并
      主动放弃过？旧仓凭什么做到过（杠杆/DD 预算/成本/腿质量逐项拆）？
      缺的是不是一条【不同结构】的腿？移植腿要顺带过泄漏审计
  例：追了很久的 1000%@DD40 正是用户团队文档里早已判定"过分激进"并改成
      DD55 的那个角；对账还顺带抓出移植 PPO 腿泄漏（真-OOS 1.75→0.07）
  详见 references/priorwork-reconcile.md
```

**🔴 盲点定律**：盲点藏在你**没看的地方**。凸性这个赢家就活在杠杆空间里原实验从没测的那一半。
每次撞墙必问：**"我测的是两个方向 / 完整空间，还是只测了显而易见的那一半？"**

### 为什么有效 / 何时停（收敛 ≠ 认输）

- **为什么有效**：每一级换的是**问题**，不是搜索力度。同一个搜索跑 10 遍还是同一个盲点。
- **诚实终点是「收敛」**：当**多个独立对抗视角都指向同一堵墙**时，才是真结论。
  - 本 session：3 次独立审查三重确认"现有数据下 2000% 不可达"→ 定性为 **forward bet**，不是放弃。
  - 收敛前每一级**仍在挖价值**；收敛 ≠ 累了放弃，是多个独立视角的一致（earned）。
- 收敛的硬判据见下方「终止条件 1」新增的【收敛判据】。

### 与既有机制的关系（不重复造轮子）

- 是「永不言弃 / 反自满条款」的**可操作版**；是「Deep Dive 深挖」的**上位框架**（Deep Dive ≈ 第 3 级）。
- 第 4 级（挑战问题本身）补全了「机制 8 跳出局部最优」——后者在空间内换方向，第 4 级跳出空间。

### 让幸存者可信的 rigor 套件（绝不放松 —— funding-bug 教训）

阶梯负责**挖**，rigor 负责**验真**。高收益没过 rigor = 回测幻觉 = 负债，不是资产。

```
□ SIGNAL-shuffle z>2    随机化"哪些币/时点带信号"（不是 return-shuffle），打散后 alpha 应消失
□ FAMILY-NULL P<0.05    加冕前过 best-of-N block-bootstrap（防多重检验 / best-of-N 幸存者偏差）
□ SEAM-leak / open(t+1) 择时信号用 open(t+1) 入场，杜绝接缝处用未来信息成交
□ TIME-SPLIT            前后两半段都要成立
□ CRASH-stress          空头波动 / 高杠杆过崩盘压力测试
□ LIQ-distance          峰值杠杆算清算距离（离强平的 headroom）
□ 基础假设不可松动       以 baseline 锁定的目标交易所 worst-case 为准
                        （示例：funding ×1 · 9bps · 因果 .shift(1)）
```

> 这套 rigor 在 baseline 里一旦定下就**全程不许改**（改假设美化数字 = 投机取巧，见"禁止投机取巧"）。
> 杀掉一打幻觉的同一套 rigor，才是敢拿幸存者上真金白银的依据。

> 📖 完整方法论（4 个案例详解 + rigor 配方 + 与各章映射）见 `references/cognitive-escalation-ladder.md`。

## 🔴 战役铁律 1-24（索引 — 详解与战例见 references/campaign-laws.md）

三轮真实战役（每轮 200+ 实验、多级对抗核查）沉淀，编号连续。命中对应场景时
必须读详解后再动手：

```
 1. OOS 开封预算制：锁箱开封 ≤3 轮 + append-only 审计留痕，最后一轮留给部署
 2. 反向假设抢救：|t|≥3 的信号禁止随错误假设弃置，必自动测反号
 3. 两级死刑庭：增量 IC（信号庭）与跨币 fold（结构庭）独立，都过才可 blend
 4. 评估器完整性核查：选币数按池比例缩放；"恰好 0%"先怀疑评估器再怀疑策略
 5. 波次脚本工程纪律：一切网格执行体必须在 __main__ 守卫内
 6. 模型分工：机械工作下放低层模型；对抗核查/设计判定/OOS 开封用最强模型
 7. 数据解锁协议：收敛后提交付费资源清单（forward bet），由用户拍板注资
 8. 幸存者偏差可修复：meta API 常永久保留下架资产，宣布"不可得"前必查
 9. 黑天鹅套件门禁：解析破产界系统性乐观，里程碑必实跑注入式压测
10. 评估器守卫默认生效：保护逻辑不许只对显式传参的调用方生效
11. 门槛必须引用锁定协议原文：禁止执行中临时加严，也禁止临时放宽
12. 历史回测红线 ≠ 合成冲击红线：杠杆上限必须用注入式压测标定
13. 组合结构（多 sleeve 资本分割）是独立假设类：ρ≈0 靠分散化，机制要诚实标注
14. 网格混杂变量纪律：测机制 X 时其余参数与冠军逐项一致 + 机制隔离对照组
15. 换手可能是 alpha 本体：降本前先做换手三项分解，再实测降换手保不保毛 alpha
16. 门槛再校准必须逐一保持全部历史判决不变，禁止为放行当前候选修门
17. headline 必须代表可部署实现：采样相位/随机种子等隐藏自由度逐一审计
18. 消灭自由度不制造 alpha：修完脆弱性仍须在更根本关卡（fold/最新段）自证
19. 独立收益流准入三件套：|ρ|<0.3 + 诚实口径正期望 + 危机期行为实测
20. 机制假设必须对照代码符号约定核验：写任务书前先 grep 符号约定
21. 代理模型凸性匹配审计：近似器与真实头寸对极端路径的响应阶数必须一致
22. 到期欧式保险不保期中回撤：保护工具触发口径必须与被保护指标一致
23. 免费数据的第二层：宣布"需付费"前先查成交打印等免费端点的重建潜力
24. "现金也是仓位"要用证据背书：空仓基线每次入网格对照——它经常赢
```

## Setup（每次新研究开始时）

和用户协商确定以下信息：

### 1. 定位项目文件
```
必须确认：
- 回测引擎入口：哪个文件/命令运行回测？（如 python backtest.py --config xxx）
- 策略代码：哪些文件可以修改？（如 strategy.py, config.py）
- 不可修改文件：引擎核心、数据加载器等（如 engine.py, data/）
- 输出格式：回测结果在哪里、怎么读取？
- 数据快照锁定：记录回测数据的截止日期与文件清单（或哈希）。研究中途禁止
  刷新数据——新数据会让所有已跑实验失去可比性；确需刷新 → 记录事件、重跑
  baseline，此前 results 全部标记为 pre-refresh era
```

### 2. 定义评估框架
```
必须确认：
- 主要优化指标（默认：Sharpe Ratio）
- 辅助指标（CAGR、MaxDD、Win Rate、交易次数等）
- 红线指标（硬约束，违反即 discard）：
  → MaxDD 不能超过 XX%
  → 单个 regime 期间亏损不能超过 XX%
  → 交易次数不能低于 XX（防止策略退化为不交易）
  → 🔴 worst_fold_cagr ≥ 0（跨币 k-fold，动态宇宙必须）
  → 🔴 generalization_ratio > 0.3（held-out fold CAGR / train fold CAGR）
- 回测时间范围（如 2022-03 ~ 2026-03）
- regime 分段定义（如 Box1=震荡期, Box2=趋势期, Box3=回撤期）
- 🔴 币种 fold 划分策略（详见机制 9）：
  → 动态宇宙策略必须在这一步就决定 k-fold 划分方案
  → 推荐策略 C（stratified by listing age 的 time-future hold-out）
  → 划分一旦锁定，全程不许改动（改了就等于窥探 held-out）
```

#### 2.x 强制区分 In-Sample vs Held-Out 指标（动态宇宙策略必做）

每次实验的 `results.tsv` 必须同时记录两套指标：

| 列 | 含义 | 用途 |
|---|---|---|
| `is_cagr` | In-sample（训练 fold）CAGR | 参考指标，**不**用于 keep/discard 判定 |
| `oos_cagr_median` | Held-out 折的中位数 CAGR | 判定主指标 |
| `oos_cagr_worst` | Held-out 折的最差 CAGR | 红线指标 |
| `generalization_ratio` | oos_median / is_mean | 过拟合检测 |

**核心原则**：任何冠军判定、阶段切换、里程碑 tag 都**只看 oos_*** 指标。
`is_*` 指标只用于诊断"为什么没泛化"，不用于"这个版本比上个版本好"的对比。

**如果 results.tsv 里还没有这些列**：优先级 P0，在做任何新实验前先补上。
旧实验可以回填（重跑 fold-level 评估），实在回填不了就标记为"pre-k-fold era"
并从冠军候选池中剔除。这是一次痛苦但必要的重置。

**心智模型切换**：量化回测的心智模型必须从"历史数据的完整回放"切换到
"ML 模型训练"。回测的时间范围和币种范围都是**训练样本**，不是完整样本。
"全历史 CAGR 3000%" 从"回放"框架看是一个结论，从"训练"框架看是一个
in-sample 指标，在 held-out 验证之前几乎不可信。详见核心原则第 7 条。

### 3. 创建实验分支
```bash
git checkout -b research/<tag>   # 如 research/mar26-chop-opt
```

### 4. 初始化 results.tsv
```
创建 results.tsv，只有 header，baseline 在第一次运行后填入：

commit	sharpe	cagr	maxdd	box1	box2	box3	score	status	hypothesis	conclusion

动态宇宙策略使用完整版 canonical schema（唯一权威列定义；2.x 与泛化优先
评估框架章节的表格是它的子集视图）：

commit	sharpe	is_cagr	is_maxdd	box1	box2	box3	oos_cagr_median	oos_cagr_worst	oos_maxdd_worst	gen_ratio	sub11_mean_cagr	sub11_worst_maxdd	score	status	hypothesis	conclusion

命名对照（消除三处文档各自表述）：机制 9 的 worst_fold_cagr = oos_cagr_worst；
泛化优先框架的 test_median_cagr / test_worst_cagr / test_worst_maxdd =
oos_cagr_median / oos_cagr_worst / oos_maxdd_worst。
keep/discard 只看 oos_* / sub11_* 列；is_* 仅用于诊断。
```

### 5. 运行 baseline
第一次实验永远是跑当前代码作为 baseline。所有后续实验都和 baseline 对比。

## 评估框架

### 复合评分公式

不能只看一个指标。使用加权复合评分，**权重根据当前研究阶段动态调整**
（详见"研究阶段策略"章节）：

```python
# 默认权重（Phase 1 增长期）
score = (
    0.30 * normalize(sharpe) +
    0.40 * normalize(cagr) +         # Phase 1 重 CAGR
    0.15 * normalize(-maxdd) +
    0.15 * normalize(regime_consistency)
)

# Phase 2（防御期）：0.35 sharpe + 0.20 cagr + 0.30 maxdd + 0.15 consistency
# Phase 3（打磨期）：0.30 sharpe + 0.20 cagr + 0.20 maxdd + 0.30 consistency

# regime_consistency = min(box1, box2, box3) / max(box1, box2, box3)
# 越接近 1 说明策略在不同市况下表现越均衡
```

⚠️ Regime 因果性警告：Box1/2/3 分段是**事后**划定的，只允许用于评估分层与
regime_consistency 诊断。如果策略内部使用 regime 信号做决策，该信号必须
T-1 因果可算，绝不允许把评估用的后验分段泄漏进策略逻辑（v14 战役 W6r 实测：
回撤 = 截面选股风险，任何 T-1 市场状态信号都预警不了——见
references/v14-campaign-lessons.md）。

### 红线守卫（violation = 立即 discard）

```
硬红线（任一违反即 discard，不管 score 多高）：
- MaxDD > baseline_maxdd * 1.2      （回撤不能比 baseline 恶化超过 20%）
- 任意 regime 收益 < baseline * 0.7  （单期不能比 baseline 差超过 30%）
- 交易次数 < baseline * 0.5          （策略不能退化为"少交易"获得假Sharpe）
- 回测报错或崩溃

动态宇宙策略附加红线（机制 9 明文要求，详见 references/anti-overfitting.md）：
- worst_fold_cagr < 0                （held-out 币 fold 亏钱 = 币种过拟合）
- generalization_ratio < 0.3         （train/test 差距过大；TRAIN 极高且每个
                                      TEST fold 都为正时按 Layer 2 判读注意处理）
- 未经预算的 OOS 开封                 （铁律 1：开封预算 ≤3 轮 + append-only
                                      审计留痕，最后一轮留给部署决策）
```

### 判定标准

```
keep    — score 提升 >= 0.5% 且无红线违反
          或 score 持平但代码更简洁（简洁性奖励，同 autoresearch）
discard — score 下降，或触及红线
trade-off — score 某些维度提升、某些下降（记录但不自动 keep，标记待人工决策）
crash   — 回测报错
```

## 研究阶段策略（三阶段渐进框架）

研究不是漫无目的地优化所有指标。根据策略当前的成熟度，研究重心应该有明确的阶段性聚焦。
每次研究开始时，先判断当前处于哪个阶段，然后按该阶段的优先级生成假设。

### Phase 1：增长期（Growth）— 主攻 CAGR

**进入条件**：CAGR 仍有明显提升空间（距离历史最佳或理论天花板 > 15%）

**核心目标**：最大化绝对收益

**策略**：
- 实验方向集中在信号灵敏度、入场时机、仓位放大、趋势捕捉等
- 此阶段可以容忍稍大的回撤（红线内），换取更高的 CAGR
- 复合评分权重偏移：

```python
# Phase 1 权重：重 CAGR，轻 MaxDD
phase1_score = (
    0.30 * normalize(sharpe) +
    0.40 * normalize(cagr) +         # ← 权重最高
    0.15 * normalize(-maxdd) +       # ← 适度放松
    0.15 * normalize(regime_consistency)
)
```

**退出信号**：连续 10+ 个实验 CAGR 提升 < 1%，或 CAGR 已触达用户设定的天花板值

### Phase 2：防御期（Defense）— 主攻回撤

**进入条件**：Phase 1 退出，CAGR 已在高位但回撤仍然显著

**核心目标**：在尽量不损失非回撤期收益的前提下，减小最大回撤和回撤持续时间

**策略**：
- ⚠️ **先创新后调参！** 优先尝试结构性创新（因子轮动、新信号、条件化选币），
  而不是直接去调杠杆/止损/仓位大小等参数。参见"创新优先于参数调优"章节。
- 创新方向：回撤期因子轮动、动量崩溃风险预测、跨资产信号、质量/流动性因子
- 参数调优方向（在创新方向探索完毕后）：止损逻辑、震荡期仓位缩减、回撤期信号过滤
- **铁律：非回撤期的收益不能显著下降**（< 5% 损失为可接受代价）
- 如果一个改动让 MaxDD 减少 5pp 但 CAGR 掉 3pp，这通常是好 trade-off
- 如果一个改动让 MaxDD 减少 2pp 但 CAGR 掉 8pp，这是坏 trade-off
- **最佳结果**：CAGR 和 MaxDD 同时改善。如果只做参数调优几乎不可能达到，
  但结构性创新经常能做到（实战已验证）
- 复合评分权重偏移：

```python
# Phase 2 权重：重 MaxDD，保 CAGR
phase2_score = (
    0.35 * normalize(sharpe) +
    0.20 * normalize(cagr) +         # ← 保住就好
    0.30 * normalize(-maxdd) +       # ← 权重最高
    0.15 * normalize(regime_consistency)
)
```

**评估技巧**：
- 将权益曲线的回撤段单独标出，对比改动前后的回撤深度和恢复速度
- 关注 Calmar Ratio（CAGR / MaxDD）作为阶段性辅助指标
- 红线调整：CAGR 不能比 Phase 1 最终值低 > 10%

**退出信号**：MaxDD 已低于目标值，或连续 10+ 个实验 MaxDD 改善 < 0.5pp

### Phase 3：打磨期（Polish）— 主攻平台期

**进入条件**：Phase 2 退出，CAGR 高、回撤小，但权益曲线仍有明显"走平"段

**核心目标**：在尽量不损失非平台期收益的前提下，缩短平台期（将横盘转为增长）

**策略**：
- 先识别权益曲线的平台期（连续 N 天收益率 ≈ 0 的区间）
- 分析平台期对应的市况：是震荡？是低波动？是假突破频发？
- 实验方向：针对平台期市况的专门信号（如震荡期网格、低波动期的均值回归）
- **铁律：非平台期（已有正收益的区间）不能显著受损**（< 3% 损失为可接受代价）
- 复合评分权重偏移：

```python
# Phase 3 权重：重 regime 一致性（消除弱 regime = 消除平台期）
phase3_score = (
    0.30 * normalize(sharpe) +
    0.20 * normalize(cagr) +
    0.20 * normalize(-maxdd) +
    0.30 * normalize(regime_consistency)  # ← 权重最高
)
```

**评估技巧**：
- 定义"平台期"指标：max consecutive days with cumulative return < X%
- 对比改动前后各 regime 的收益分布，确保正收益 regime 不被拖累
- 关注 regime_consistency 指标提升（各段表现更均衡 = 平台期被填补）

**退出信号**：权益曲线已接近理想形态（平滑上升），或无法再缩短平台期

### 阶段判定与切换

```
研究开始时的阶段判定流程：

1. 运行 baseline，获取指标
2. 判断阶段：
   - CAGR < 用户目标值 * 0.85        → Phase 1（增长期）
   - CAGR >= 目标值 * 0.85 且 MaxDD > 目标值  → Phase 2（防御期）
   - CAGR 达标 且 MaxDD 达标 且有明显平台期    → Phase 3（打磨期）

3. 在 results.tsv 的 hypothesis 列标注当前阶段：
   [P1] lower entry threshold for more trades
   [P2] add trailing stop in drawdown regime
   [P3] enable mean-reversion in low-vol plateau

4. 阶段切换时，重新计算 score（用新阶段的权重），
   但保留旧阶段的 results 记录供参考
```

**重要**：阶段不是严格线性的。如果 Phase 2 的某个实验意外让 CAGR 大幅跳升，
可以回到 Phase 1 继续挖掘增长空间。灵活判断，但每个实验必须有明确的阶段归属。

## 实验循环

**LOOP FOREVER（直到人工中断）：**

### Step 1：形成假设

在修改代码之前，**必须写出书面假设**，并标注类型：

```
类型：[INNOVATION] / [PARAM-TUNE]
来源：Innovation Backlog #3 / Deep Dive 发现 / 上一实验衍生 / 近失回收
阶段：[P1] / [P2] / [P3]

假设：引入 Amihud 非流动性因子作为 bear regime 的选币过滤器
预期效果：
- Bear 期：正面——过滤掉低流动性币种，减少滑点和假信号
- Bull/Chop 期：中性——高流动性币本就是动量策略的主力标的
- 综合预期：MaxDD 改善 3-5pp，CAGR 轻微下降 1-2pp（可接受的 trade-off）
依据：Bear 市低流动性币波动更剧烈且流动性骤降，是回撤的主要来源
创新点：改变了 bear regime 的选币逻辑，不只是调仓位大小
```

**假设质量检查**（写完后自问）：
- 这个改动会让策略在不同的时间做出不同的决策吗？→ 是 = 创新，否 = 调参
- 如果标注了 [INNOVATION] 但答案是"否"，诚实改标为 [PARAM-TUNE]
- 如果连续 3 个假设都是 [PARAM-TUNE]，停下来问：
  "Innovation Backlog 里有没有该做的创新？为什么在回避？"

为什么要写假设：
- 防止盲目调参（"试试看把这个改成 0.3"不是假设）
- 实验结果和预期对比时能学到东西
- 如果假设频繁和结果不符，说明你对策略的心智模型有误，需要停下来重新理解
- **强制区分创新和调参，保持科研的主动性**

### Step 2：修改代码

只修改约定范围内的文件。改动要小且聚焦——**每次实验只改一个变量**。

```
好的实验设计：
✅ 只改 chop_floor: 0.45 → 0.40（单变量）
✅ 只加一个新模块（如 V32 smart boost），其他不动
✅ 只删一个模块，看 score 是否不变（简洁性测试）

坏的实验设计：
❌ 同时改 chop_floor + safe_cap + decline_mult（多变量，无法归因）
❌ "大幅重构策略"（无法和 baseline 对比）
```

### Step 3：git commit

```bash
git commit -m "exp: [假设摘要]"
# 如: git commit -m "exp: lower chop_floor 0.45→0.40 for more chop trades"
```

### Step 4：运行回测

```bash
# 将输出重定向到文件，不要让输出淹没 context
[backtest command] > run.log 2>&1
```

读取关键指标：
```bash
# 根据项目具体的输出格式提取（这里是示例）
grep -E "sharpe|cagr|maxdd|box" run.log
```

### Step 5：评估结果

1. 提取所有指标
2. 检查红线（任一违反 → 立即 discard）
3. 计算 composite score
4. 与 baseline（或当前最佳）对比
5. 对比假设和实际结果——**写出结论**

### Step 6：记录到 results.tsv

```
commit	sharpe	cagr	maxdd	box1	box2	box3	score	status	hypothesis	conclusion
a1b2c3d	1.762	130.4	28.1	+133.6	+45.2	-23.0	1.000	keep	baseline	baseline established
b2c3d4e	1.876	144.3	29.5	+159.8	+42.1	-20.8	1.065	keep	lower chop_floor 0.45→0.40	hypothesis confirmed: Box1 +26pp, Box3 +2pp, slight MaxDD increase acceptable
c3d4e5f	1.821	138.7	30.2	+148.3	+40.5	-22.1	1.031	discard	add V35 vol adaptive	worse than b2c3d4e, V35 conflicts with V32 as suspected
d4e5f6g	0.000	0.0	0.0	0	0	0	0.000	crash	double model width	OOM during backtest
```

**注意：results.tsv 不要 git commit，留在 untracked 状态**（同 autoresearch）。

### Step 7：keep 或 discard

```
如果 keep：保留 commit，继续在此基础上迭代
如果 discard：git reset --hard HEAD~1，回到上一个 keep 点
如果 trade-off：保留 commit 但在 tsv 标记 trade-off，继续用上一个 keep 点迭代
如果 crash：修 bug 重试一次，还是 crash 就放弃，git reset
```

### Step 8：回到 Step 1（带科研主动性要求）

**生成下一个假设时，必须按以下优先级顺序选择，不能跳级：**

```
优先级 1（最高）：Innovation Backlog 中未尝试的结构性创新
  → 先看 backlog，里面一定有你还没试的创新方向
  → 如果 backlog 为空，立即触发"Deep Dive 深挖" / 沿【认知升级阶梯】逐级爬升（见下方）补充

优先级 2：上一个实验的结论衍生出的新创新方向
  → 一个失败的创新实验也有价值——它暴露了哪些假设是错的？
  → 问："如果换一种方式实现同一个创新思路呢？"
  → 一个创新方向至少尝试 3 种不同的实现方式再放弃

优先级 3：之前 discard 的实验中接近成功的"近失"回收
  → 在新的代码基础上重试之前差一点成功的实验

优先级 4：简洁性测试 / 阶段切换检查

优先级 5（最低）：参数调优
  → 只在优先级 1-4 全部穷尽后才做
  → 标注 [PARAM-TUNE]，保持比例 < 40%
```

**各阶段的创新方向指引（不是完整列表——你应该发掘更多）：**

```
Phase 1（增长期）— 创新方向：
  ✅ 引入新因子类型（均值回归、量价关系、链上数据、跨资产信号）
  ✅ 改变因子组合方式（线性加权 → 非线性、排序 → 分位数、固定 → 自适应）
  ✅ 改变选币逻辑（top-N → 条件过滤、等权 → 信号强度加权）
  ✅ 改变入场/出场信号结构（连续 → 离散、阈值 → 突破确认、单一 → 多重确认）
  ⚠️ 信号灵敏度调整、仓位放大 = 参数调优，不是创新

Phase 2（防御期）— 创新方向：
  ✅ 回撤期因子轮动（不同市况用不同因子组合）
  ✅ 动量崩溃风险预测信号（新的风险因子）
  ✅ 跨资产风险信号（BTC 波动率、资金费率异常、相关性突变）
  ✅ 自适应 regime 切换（不是人工阈值，而是数据驱动的检测）
  ⚠️ 调杠杆、加止损、回撤减仓 = 参数调优，不是创新

Phase 3（打磨期）— 创新方向：
  ✅ 平台期专门信号（低波动期均值回归、震荡期网格逻辑）
  ✅ 时间周期自适应（不同市况用不同的 K 线周期）
  ✅ 仓位信号化（仓位大小由信号强度决定，而非固定比例）
  ⚠️ 调整现有参数的数值 = 参数调优，不是创新
```

### Deep Dive 深挖（Innovation Backlog 补充机制）

**当 Innovation Backlog 为空或感觉"想法用完了"时，不是停止的理由，
而是需要更深入地挖掘。**

> 💡 **撞墙时优先按【🔴 认知升级阶梯】逐级爬**（见上方同名章节）。Deep Dive 是阶梯的一环，
> 主要对应第 3 级「完备性批判」（回溯搜索盲区）；下面 5 步是完备性批判的具体动作清单。

```
Deep Dive 流程（每次耗时 10-15 分钟，不跑回测，纯思考+阅读）：

1. 重读策略代码
   → 逐行阅读核心策略文件，不是扫一眼
   → 关注：被注释掉的代码、TODO 标记、已禁用的功能
   → 问自己："这段代码为什么这样写？有没有完全不同的写法？"

2. 审视权益曲线
   → 找到权益曲线上每一个"意外"：
     - 大跌之前发生了什么？策略有信号但没行动？
     - 大涨期间策略是满仓还是轻仓？为什么？
     - 平台期期间策略在做什么？是不交易还是交易但亏赢相抵？
   → 每个"意外"都可能是一个创新假设的起点

3. 逆向思维
   → "如果我要故意让这个策略变差，我会怎么改？"
   → 答案的反面可能就是改进方向
   → "如果这个策略在 2027 年彻底失效了，最可能的原因是什么？"
   → 答案就是你应该去解决的脆弱性

4. 跨领域灵感
   → 其他策略类型的思路能不能借用？
     - 做市策略的库存管理 → 仓位管理的新思路
     - 套利策略的价差检测 → 新的入场信号
     - 期权策略的波动率建模 → 新的风险因子
   → 传统金融的方法能不能用到加密市场？
     - Fama-French 因子、动量生命周期、流动性溢价
   → 🔴 联网搜前沿（没思路时的第一动作，不是最后动作）：
     - arXiv / GitHub / papers-with-code 上 2024-26 的量化与 AI 新成果
     - 非金融领域的 RL / ML / DL / 自监督 / 序列模型 / 新架构——很多 alpha 来自跨域迁移

5. 补充 Innovation Backlog
   → Deep Dive 结束后必须至少补充 5 个新的创新假设
   → 如果想不出 5 个，说明你对策略的理解还不够深——继续读代码
```

**Deep Dive 触发时机**：
- Innovation Backlog 见底时（强制触发）
- 每累计 15 个实验后（定期触发，即使 backlog 还有内容）
- 连续 3 个创新实验都 discard 后（可能方向不对，需要重新思考）

### Innovation Backlog 管理

在研究过程中持续维护一个创新想法清单（保存在 research plan 中）：

```
Innovation Backlog（示例）：
□ [P1] 引入 Amihud 非流动性因子作为选币过滤器
□ [P1] 尝试因子得分的非线性组合（如 RankIC 加权）
□ [P2] 构建动量崩溃预警信号（基于 momentum reversal 速度）
□ [P2] 利用 BTC dominance 变化作为 regime 切换信号
□ [P3] 平台期自动切换到 mean-reversion 子策略
□ [P3] 引入交易量 profile 检测假突破
✅ [P1] 因子轮动（已完成，keep）
✗ [P2] 波动率 targeting（已完成，discard — 与现有模块冲突）

规则：
- 新想法随时添加，不要"等到合适的时候"
- 完成的标 ✅（keep）或 ✗（discard + 原因）
- Backlog 中至少保持 5 个未尝试的想法
- 低于 5 个 → 触发 Deep Dive
```

## 启动协议

**Alpha Lab 必须由用户手动启动，不能自动触发。**

启动时执行以下协议：

### Step 0：启动前问答（一次问完，不要分多轮）

**科研过程是无人值守的，所有需要用户确认的事项必须在启动前全部问清。**

启动前向用户确认以下所有信息（一次性提问，附默认值供用户快速确认）：

```
必问项（每次都问）：

1. 研究时间预算
   "本次研究的最长时间预算是多少？"
   默认：4 小时
   记录为 max_research_time

2. 回测周期
   "回测覆盖多长的历史数据？"
   默认：近 4 年
   记录为 backtest_period

3. 目标 CAGR
   "目标年化收益率（CAGR）是多少？用于判断 Phase 1 是否到达天花板。"
   默认：200%
   记录为 target_cagr

4. 目标 MaxDD
   "可接受的最大回撤上限是多少？用于 Phase 2 判定和红线设置。"
   默认：25%
   记录为 target_maxdd

5. 研究范围
   "本次研究的重点方向或约束？（如：只优化震荡期、不动 XX 模块、重点测试 XX 功能）"
   默认：无约束，全方位优化

6. 回测命令与文件
   "确认回测命令和可修改的文件范围？"
   （如果 session-memory 中有上次的配置，展示给用户确认即可）
```

```
视情况追问（根据项目实际情况判断是否需要）：

7. Walk-Forward 验证方式
   "防过拟合验证选哪种方式？
    方式一（推荐）：保留最后 N 个月做 OOS，日常实验只用前面的数据
    方式二（更严格）：滚动 Walk-Forward，每个窗口都验证
   默认：方式一，保留最后 9 个月
   记录为 oos_months

8. Regime 分段定义
   如果用户没有预定义 regime，需要讨论如何分段

9. 额外红线
   用户是否有特殊约束（如：交易频率上限、单笔最大仓位等）

10. 偏好的实验方向
   用户是否有特别想尝试或特别想避免的方向
```

### Step 1：申请权限与环境检查

**科研期间无人值守，所有权限必须提前获取，不能中途卡住等用户。**

```
权限检查清单：

□ 文件读写权限
  → 确认策略代码、配置文件、回测引擎所在目录可读写
  → 确认 results/ 目录可写（用于保存 dashboard）

□ Git 权限
  → 确认可以创建分支、commit、tag
  → 创建研究分支：git checkout -b research/<tag>
  → 测试一次 git commit 是否正常工作（处理可能的 lock 文件问题）

□ 回测命令可运行
  → 先跑一次回测确认命令正确、输出格式可解析
  → 记录回测耗时，用于更精确的产能估算

□ Dashboard 生成环境
  → 确认 comparison-dashboard skill 可用

□ 网络 / API（如果回测需要）
  → 确认回测不依赖实时数据，或相关 API 可访问
```

如果任何权限或环境检查失败，**在启动前就解决**，不要带着问题进入无人值守循环。

### Step 2：确认启动并报告预估

```
所有信息确认后，向用户汇报：

"启动参数确认：
 - 时间预算：X 小时
 - 回测周期：近 X 年（YYYY-MM ~ YYYY-MM）
 - 目标 CAGR：X%  |  目标 MaxDD：X%
 - 研究范围：[描述]
 - 预估产能：约 XX-XX 个实验

 环境检查全部通过 ✅ 现在开始研究。"
```

### 启动后：自主运行，绝不中断询问

一旦启动，**不要暂停询问用户是否继续**。用户可能在睡觉。
你是自主研究员。如果想法用完了，重新读代码、读之前的实验结果寻找灵感、
尝试更大胆的改动。

**唯一允许中断用户的情况**：回测引擎崩溃且无法自行修复（如数据文件损坏、
依赖服务挂了）。此时输出已完成的成果，说明中断原因，等待用户介入。

## 终止条件

研究停止**只有两种情况**，没有第三种：

### 终止条件 1：找到全局最优

判定标准（**全部**满足才能宣布，不是"至少 3 条"）：
```
□ Innovation Backlog 清空：所有记录的创新想法都已尝试或有明确的排除理由
□ Deep Dive 至少做过 3 次：且最后一次 Deep Dive 未能产出有效的新方向
□ 结构性创新已覆盖至少 5 个不同的维度：
  （如：新因子、因子组合方式、选币逻辑、regime 切换、风控信号）
  → 不是 5 个实验，是 5 个维度，每个维度可能有多个实验
□ 每个创新维度至少尝试过 2 种不同的实现方式再放弃
□ 连续 15+ 个实验 score 提升 < 0.2%（含创新实验，不能只是参数调优凑数）
□ 已触发过至少 2 次"跳出局部最优"机制且均未找到更好的解
□ 简洁性审计通过：删除任何模块都会导致 score 下降
□ 创新比例达标：整个研究过程中 [INNOVATION] >= 60%
```

**穷尽创新审计**（在宣布全局最优前必须完成）：
```
回答以下每个问题，如果任一回答"否"→ 还不能宣布全局最优：

1. "我是否尝试过至少 2 种完全不同的因子类型？"
   （不是调参数，而是引入新的 alpha 来源）

2. "我是否尝试过改变因子的组合/加权方式？"
   （线性 → 非线性、等权 → 信号强度加权等）

3. "我是否尝试过改变选币/入场逻辑的结构？"
   （top-N → 条件过滤、固定阈值 → 自适应阈值等）

4. "我是否尝试过利用跨资产/跨周期信息？"
   （BTC 信号、多周期融合、链上数据等）

5. "我是否深入分析过权益曲线上每个'意外'并针对性设计实验？"
   （大跌、平台期、错过的大涨——每个都试过改善了吗？）

6. "对于每个失败的创新方向，我是否至少尝试了 2 种不同的实现方式？"
   （一种实现失败不代表方向错误，可能只是实现方式不对）

7. "我最后一次 Deep Dive 是什么时候？距现在超过 10 个实验了吗？"
   （如果超过了，再做一次 Deep Dive 再决定）
```

**收敛判据（诚实终点的硬标准，强于"7 问全 yes"）**：

穷尽创新审计 7 问全"是"还不够——宣布"全局最优"前还要满足【收敛】：
```
□ 至少 3 个【相互独立】的对抗视角，都指向同一堵墙
  （独立 = 不同的问题/数据/方法，不是同一个搜索重跑 3 次）
□ 每个视角在确认墙之前，都先尝试挖掘并给出了结构性证据（不是空手认输）
□ 这堵墙是"现有数据/约束下不可达"，而非"我懒得再想了"
  → 是前者：明确写出"换什么数据/放松什么约束能突破"（作为 forward bet 的条件）
```
收敛 ≠ 认输：认输是单一视角的疲劳，收敛是多个独立视角的一致。
实证：3 次独立审查三重确认"现有数据下 2000% 不可达"→ 定性为 forward bet（详见「🔴 认知升级阶梯」）。

达到此状态后，**先执行"机制 10：最终上线验证协议"（The Final Gate，见
references/anti-overfitting.md）**。
只有通过 Final Gate（⭐⭐ 以上）才能宣布全局最优。
如果 Final Gate 未通过（⭐），说明找到的是过拟合的假最优，
需要根据失败项回溯修正（如降低参数敏感度、处理 decay 问题），然后继续研究。

通过 Final Gate 后，执行"研究终止交付流程"。

### 终止条件 2：到达最长研究时间

```
当 elapsed_time >= max_research_time 时：

⚠️ 不要立即中断！必须完成当前迭代的完整流程：
1. 如果正在跑回测 → 等回测完成，评估，记录
2. 如果刚发现了一个 keep → 触发里程碑检查点（dashboard + review + commit）
3. 如果正在修 bug → 修完再停

完成当前迭代后，执行"研究终止交付流程"（见下方），
标注"因时间预算用尽而停止"，并列出"如果有更多时间，下一步会尝试什么"。
```

**注意**：除以上两种情况外，**绝对不要**因为"感觉差不多了"而自行停止。
用户给了时间预算就要用满，除非已确认全局最优。

**永不言弃的铁律**：
- 时间预算没到 + 目标没达到 = 继续研究，没有例外
- "所有想法都试完了" 不是停止的理由——做 Deep Dive，逼自己想出新的
- "连续 N 次失败" 不是停止的理由——这说明需要更大胆的创新，而非放弃
- "结果已经比 baseline 好很多了" 不是停止的理由——好很多 ≠ 全局最优
- 只有"穷尽创新审计 7 个问题全部回答'是' + Final Gate 通过"才是合法的终止

## 里程碑检查点（摘要 — 完整流程见 references/milestone-and-delivery.md）

触发条件（任一）：score 较上一里程碑提升 ≥5% / 完成阶段切换 / 成功跳出局部
最优 / 每累计 20 个 keep / Level 3 结构性重构前（保存安全回退点）。

六步流程（触发时 Read 完整版逐步执行，不可跳过）：
1. comparison-dashboard 生成对比看板（当前最佳 vs baseline vs 上一里程碑）
2. 过拟合 & Decay 专项门禁：2a 参数稳定性（联合扰动 CV<0.10 ✅ / >0.20 🔴）、
   2b Decay Ratio（>0.85 ✅ / <0.60 🔴）+ rolling Sharpe 斜率、2c regime 一致性、
   2d 验证结果写入 results.tsv。任一 🔴 → 里程碑标 ⚠️ UNSTABLE，不作为安全
   回退点，必须先修正再继续
3. quant-code-review（Full 模式）全面审计
4. 修复审计发现的问题（修复后重跑回测确认 score 未破坏）
5. git commit + tag milestone-N（安全回退点，commit message 含指标+检查结果）
6. session-memory 保存

**里程碑版本是神圣的**：必须通过 code review，是后续实验的回退锚点。

## 研究终止交付流程（摘要 — 完整模板见 references/milestone-and-delivery.md）

终止时五步全做（铁律，缺一不可）：
1. 最佳版本代码就绪：checkout 到 score 最高版本 → 必要时补一次 code review →
   git tag best-{research-tag}，commit message 记录完整指标
2. 最终 Dashboard（comparison-dashboard）：权益曲线对比（对数坐标）/ 分 regime
   柱状图 / 里程碑演进 / rolling Sharpe 趋势 / Decay Ratio / Final Gate ⭐ 摘要
3. 研究总结报告：终止原因 / 指标对比（必须并列 IS 与 OOS 两列）/ Final Gate
   结果 / 实验统计 / 阶段历程 / 关键发现 / 失败路径 / 下一步建议
4. present_files 交付：dashboard + 报告 + results.tsv + best tag 名称
5. session-memory 保存：关键发现 / 最佳 tag / 失败路径，供下次会话无缝继续

## 与其他 skill 的协作

- **comparison-dashboard**：里程碑检查点时触发 + 研究结束时触发，生成可视化看板
- **quant-code-review**：里程碑检查点时触发（用 Full 模式），对当前代码做全面
  审计，发现问题立即修复。其 2.12-2.14（动态宇宙覆盖 / realism flag / 过拟合
  参数稳健性）正是 alpha-lab champion 的四重验收对接点；其 1.4 部署缺口检查
  负责确认"champion 真的接入了实盘信号流"
- **codex-audit**：多轮独立审计协议的 Round 3 开放式审计可由它执行（独立 AI
  审计员）；finding 按 Bug/Preference 分类 + EV 算账（quant-code-review 6.5）
- **session-memory**：里程碑检查点时保存 + 研究结束时保存，确保跨会话上下文不丢失
- **git-cleanup**：研究结束后触发，清理工作区，确保 results.tsv 等临时文件不被误提交

## Reference（索引 — 按需加载，勿全量读入）

| 文件 | 内容 | 何时加载 |
|---|---|---|
| research-template.md | 研究计划模板（含 Final Gate 表 / 阶梯日志 / rigor 清单） | Setup 时复制填写 |
| research-ethos.md | 科研精神 / 反自满 / 创新优先完整版 | 每 10 实验检查点、想停下来时 |
| continuous-loop-protocol.md | 持续运行协议完整版：实测失败案例 + 根因 + 机械修法 | 回合空转/想收尾等用户时、可观测性 bug 排查时 |
| campaign-laws.md | 战役铁律 1-24 详解 + 战例 | 索引命中对应场景时 |
| anti-overfitting.md | 防过拟合机制 1-10 + 泛化优先三层评估 | 里程碑门禁、冠军判定、Final Gate |
| milestone-and-delivery.md | 里程碑检查点 + 终止交付完整流程 | 里程碑触发时、研究终止时 |
| failure-case-library.md | 经典失败案例 1-6（方法论层 + 工程层） | 里程碑时翻一遍防重蹈覆辙 |
| audit-protocols.md | 事故调查 / 多轮独立审计 / 修复回归预防 / 四类审计观点 | 实盘出事、Final Gate 前、修关键 bug 后 |
| parity-and-production.md | 数据真实性铁律 / 实盘-回测对齐 / 研究 vs 生产语义鸿沟 | Setup 数据检查、部署前对齐审计 |
| cognitive-escalation-ladder.md | 认知升级阶梯完整版 + 实证案例 + rigor 配方 | 撞墙时 |
| v14-campaign-lessons.md | 三轮战役完整战报（铁律出处） | 铁律出处考证、类似战役场景 |

研究模板见 `references/research-template.md`。
每次新研究开始时复制此模板作为研究计划。

v14 战役完整沉淀（v3 新增铁律的战例与推导）见 `references/v14-campaign-lessons.md`。

认知升级阶梯方法论完整版见 `references/cognitive-escalation-ladder.md`——
撞墙时（搜索穷尽 / 没 alpha）按此逐级爬升，含本-session 实证案例与 rigor 套件配方。

### 新增方法论参考（road-to-2000 弧线，2026-07）

撞墙→提认知→穷尽实践的一轮沉淀。7 个参考文档，撞到相应场景时读：

- `references/ceiling-law.md` — exp(Sharpe²/2) CAGR 天花板 + CAGR@DD 反解。宣布"够不到 X% CAGR"前先算理论上界（1000% 需 Sh≥2.19@Kelly；DD≤55 需 2.5-2.7）。
- `references/inflation-patterns.md` — 8 类回测虚高/泄漏目录（gate-bypass / funding×N / 静态-tilt lookahead / dredge / 年化错配 / accrual-无-MTM / 多重比较 / ML样本内膨胀）+ 加冕前 8 项 checklist。
- `references/leakage-audit-mlrl.md` — ML/RL 腿泄漏审计：train/test 重叠陷阱、peek-ahead 探针（shift(-1) 暴涨=因果签名）、真-OOS=所有训练截止之后。
- `references/orthogonal-structures.md` — N_eff / 正交『结构』≠正交『信号』（TS动量⊥XS⊥宏观择时⊥carry⊥RL）/ 等权(1-N)打败均值方差 OOS（DeMiguel）。
- `references/leg-mining-harness.md` — 手工搜索穷尽 → 规模化：legkit 共享 harness（agent 只写信号）+ 并行穷尽 leg-mining workflow + family-null best-of-N（含全部死构造）。
- `references/priorwork-reconcile.md` — 认知升级阶梯第 5 rung（向内/旁求）：撞墙或宣布墙之前，与用户自己的 prior work / sibling 仓对账。
- `references/deployable-discipline.md` — 研究 Sharpe vs 可部署 Sharpe：回测构造 vs live 因果可算 + 跨仓腿依赖=部署断点。上真金前每部件须有因果 live builder + 机器零 parity。
