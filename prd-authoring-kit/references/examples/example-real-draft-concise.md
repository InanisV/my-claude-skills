# 简洁参考（极简粒度基准）

> 这是一份**极简粒度的示意原稿**，用来校准粒度——真实 PM 写得极简。补门禁必需的章节（成功指标/验收/目标用户/本期不包含/埋点）时，**在这个简洁度上最小增补**即可，别把每节写成长文，别塞没人确认的逻辑。示意原稿全文如下（几乎就这些）：

## Background & Business Values 背景与价值
| 字段 | 内容 |
|---|---|
| 背景 | 运营提出把某列表页的初始默认 Tab 从「Tab A」调整为「Tab B」 |
| 价值 | 提升业务量 |
| 需求来源 | @产品负责人 · 优先级 P1+ |
| jira | iOS APP-XXXXX / Android APP-XXXXX / Web WEB-XXXXX |

## Overall User Scenarios 用户场景总览
（一张 mindnote 脑图）

## User Stories 用户故事
### US1: 作为运营，想通过设置初始默认 Tab 来提升曝光及业务量
#### US1-R1: 优化初始默认为 Tab B
##### 需求逻辑：
**Web/H5（列表页 X、列表页 Y）**
1. web 端列表页 X、列表页 Y 初始默认选中为 Tab B；
2. 列表其他逻辑不变；其他列表逻辑也不变；
3. 兜底：如果后端获取 Tab B 数据失败，默认回退到 Tab A。

**APP（列表页 X、列表页 Y）**
1. APP 端列表页 X、列表页 Y 初始默认 Tab 为 Tab B；
2. 列表其他逻辑不变；其他列表逻辑也不变；
3. 兜底：如果后端获取 Tab B 数据失败，默认 Tab 回退到 Tab A。

##### 兜底逻辑：不涉及，省略。

## Data Requirements（原稿：埋点待处理，空表）
## Localisation Requirements（原稿：一个 i18n sheet 嵌入）
## PRD Quality Gates（定稿前删）：隐私 / 超长 / 登录态 / 弱网断网 / 双端统一 / 手势 / 异步 等勾选清单

---

## 粒度启示（写作时对照）
- **需求逻辑 = 每端 3 条 bullet**；**兜底 = 一句"失败→回退默认 Tab"**。
- **没有** 超时具体值、config 开关、会话状态保持、双重失败降级、术语长篇——**这些都交给研发**，PM 不写。
- 弱网/断网/登录态/超长/双端 这些"考虑了吗"，原稿用**末尾 Quality Gates 勾选清单**一次性表态，**不在需求逻辑里逐条展开**。
- 门禁要的 **成功指标 / 验收 / 目标用户 / 本期不包含**，在此简洁度上**最小增补**（成功指标 1 主指标——护栏默认不加、涉资金/客诉风险可选 1 条，见 `01 §3`；验收几条关键断言）；凡我方推断补的，标 **（推断/待产品确认）**，别写成产品已定。
