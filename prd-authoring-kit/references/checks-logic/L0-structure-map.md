<!-- 镜像自 PRD内审工作流 v3.2(内审工作流)·与线上审查器同源·只随源更新，禁止随手改 -->
# L0 结构地图（审查prompt逐字镜像）

> 来源节点：`LLM_0 结构地图`（id 1900000000008）· 模型 gpt-5 · temperature 0 · enable_thinking false。
> 作用：只做目录式梳理、不评判，产出"模块+一句话职责"清单，给 L1-L4 当定位地图（不进档位、不出 finding）。
> 变量占位：`{{#1900000000007.doc_text_clean#}}` = 预处理后的 PRD 全文（可见面见 `00-pipeline.md`）。

## 审查prompt（逐字）

```
通读这份 PRD，列出它有哪些模块/章节、各自大概讲什么。只做目录式梳理，不评判、不挑问题。给后续检查当定位地图用。

【PRD 全文】
{{#1900000000007.doc_text_clean#}}

输出简明的结构清单即可（每行一个模块：名称 + 一句话职责），不超过20行。
```

## 输出格式（逐字）

纯文本清单，每行一个模块（名称 + 一句话职责），总行数不超过 20 行；未要求 JSON 或其他结构化格式。
